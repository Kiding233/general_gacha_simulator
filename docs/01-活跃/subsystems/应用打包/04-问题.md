# 应用打包 -- 问题

> 创建日期：2026-06-04（首次建档）
> 最后更新：2026-06-04（首版实施后更新）

---

## P0（阻塞性）

| 编号 | 问题 | 状态 |
|------|------|------|
| — | 暂无 | — |

## P1（风险性）

| 编号 | 问题 | 状态 |
|------|------|------|
| P1-1 | 完整 GUI 功能测试——多进程模拟（4 Worker）、Plotly 图表渲染、用户数据目录（`%APPDATA%/GachaStat/`）、配置导入导出、窗口图标、版本号。构建警告均为跨平台兼容性问题（pwd/grp/posix 等 Unix-only 模块），无功能影响。启动测试已通过（ImportError 链路清除，GUI 初始化成功），但需在桌面环境中验证完整功能。 | 待桌面验证 |
| P1-2 | QtWebEngine Chromium 资源文件——`Qt6WebEngineCore.dll` (196MB) 已打包，`QtWebEngineProcess.exe` 及资源文件已由 PyInstaller hook 自动收集。ChartWebView 在无图形环境中报加载失败属预期行为。 | 待桌面验证 |
| P1-3 | 杀软误报风险——文件夹模式下缓解但未消除。714MB 的 `.exe` + 数百个 DLL 可能触发启发式扫描。 | 待评估 |

## P2（债务性）

| 编号 | 问题 | 状态 |
|------|------|------|
| P2-1 | 产物体积偏大（714MB vs 预估 180-250MB）——主要贡献：PyQt6/Qt6 478MB（含 QtWebEngineCore 196MB）、scipy 55MB、numpy.libs 21MB。QtWebEngine 和 scipy 均为必要依赖，当前无有效缩减手段。PIL/lxml/statsmodels/pandas 为 matplotlib/scipy 的传递依赖，排除风险高。 | 搁置 |
| P2-2 | macOS/Linux 打包配置未建立 | 搁置（首版仅 Windows） |
| P2-3 | CI/CD 自动构建未配置 | 搁置（手动构建先验证） |
| P2-4 | 版本号自动注入 `.spec` 的 `name` 字段 | 搁置（当前手动同步） |
| P2-5 | 精简版（CLI only）打包方案未探索 | 搁置 |

## 已解决

| 编号 | 问题 | 解决方式 |
|------|------|---------|
| ✅ | scipy 隐藏导入不全 | 5 个 C 扩展全部正确收集，无缺失 |
| ✅ | visualization 惰性导入 | hiddenimports 覆盖，构建通过 |
| ✅ | chart_container.html 死代码 | 已删除（git rm） |
| ✅ | spec 缺少 COLLECT | 已补充——单文件 → 文件夹分发 |
| ✅ | distutils 排除冲突 | 已从 excludes 移除 |
| ✅ | unittest 被 numpy.testing 依赖 | 已从 excludes 移除 |

---

*04-问题在首版打包完成后根据实际遇到的问题更新。*
