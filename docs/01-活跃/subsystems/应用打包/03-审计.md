# 应用打包 -- 审计

> 创建日期：2026-06-04（首次建档）
> 最后更新：2026-06-04（首版构建后填写可确认项，桌面验证项待实际环境）

---

## 审计清单

| 序号 | 检查项 | 预期 | 实际 | 状态 |
|------|--------|------|------|------|
| 1 | `paths.py` 开发环境回退正确 | `get_config_dir()` → `<project_root>/gacha_simulator/config/` | 终端验证通过——config_dir/resource/user_data_dir 三个路径均正确解析且 os.path.exists 为 True | ✅ 通过 |
| 2 | `paths.py` 打包环境路径正确 | `get_config_dir()` → `sys._MEIPASS/config/` | 产物结构确认：`_internal/config/` 含全部 7 个文件 + pools/ 子目录，`_internal/resources/app_icon.png` 存在 | ✅ 结构正确（运行时待桌面验证） |
| 3 | `main.py` freeze_support 位置 | `if __name__` 护卫块首行 | 代码审查确认——`from multiprocessing import freeze_support; freeze_support()` 在 `if __name__ == '__main__':` 块第 2-3 行，位于其他 import 之前 | ✅ 通过 |
| 4 | spec 入口点正确 | 产物 `GachaStat.exe` 能启动 GUI | 终端执行——ImportError 链路已清除，GUI 初始化成功（QApplication + MainWindow 创建），QWebEngineView 在 headless 环境报渲染错误属预期 | ✅ 通过（完整 GUI 待桌面验证） |
| 5 | 配置文件打包完整 | `config/` 下 7 个文件 + `pools/` 子目录均存在 | `_internal/config/` 含 cards.txt, gains.txt, initial_resources.txt, pity.txt, resources.txt, schedule.txt, targets.txt + pools/ | ✅ 通过 |
| 6 | 资源文件打包完整 | `resources/app_icon.png` 可加载 | `_internal/resources/app_icon.png` 存在 | ✅ 通过 |
| 7 | QtWebEngine 功能正常 | Plotly 图表在 WebEngineView 中渲染 | `plotly.min.js` 已打包至 `_internal/plotly/package_data/`。Headless 环境 QWebEngineView 无法渲染（预期），需桌面验证 | ⚠️ 待桌面验证 |
| 8 | multiprocessing 正常 | 多 Worker 模拟无 `freeze_support` 错误 | `freeze_support()` 代码正确放置，`multiprocessing` 运行时 hook 已由 PyInstaller 注入（`pyi_rth_multiprocessing.py`）。实际多 Worker 模拟需桌面环境验证 | ⚠️ 待桌面验证 |
| 9 | scipy 导入正常 | 统计分析面板无 ImportError | scipy.stats 导入链路正常（`comparison_analyzer.py` 的 `from scipy import stats` 成功），5 个 C 扩展 hiddenimports 全部命中 | ✅ 通过 |
| 10 | 无控制台黑窗 | `console=False` 生效 | spec 中 `console=False`，PyInstaller 使用 `runw.exe` bootloader（Windows 无控制台变体） | ✅ 通过 |
| 11 | 版本号正确 | 关于页面显示当前版本 | `gacha_simulator._version.__version__` 导入成功（GUI 初始化中 `app.setApplicationVersion(__version__)` 执行），关于对话框内容待桌面确认 | ⚠️ 待桌面验证 |
| 12 | 产物体积合理 | ~700-750MB（修订后） | 实际 ~714MB：PyQt6/Qt6 478MB（QtWebEngineCore 196MB）+ scipy 55MB + numpy.libs 21MB + 其他 160MB。体积在 QtWebEngine+科学计算栈正常范围内 | ✅ 通过 |

---

## 桌面环境待验证（3 项）

需在 Windows 桌面环境中实际双击 `GachaStat.exe` 完成：

| 检查项 | 审计编号 |
|--------|---------|
| Plotly 图表渲染（QtWebEngine） | #7 |
| 多 Worker 模拟（multiprocessing） | #8 |
| 关于对话框版本号显示 | #11 |
| 13 项验证清单（含新增 #12 所有分析可用 + #13 导入导出功能可用） | 全部 |

---

*审计文档随桌面验证结果持续更新。*
