# 流式分析 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `SharedResultCollector.on_result()` 的单线程使用验证——确认所有调用路径均在主进程/主线程中串行执行
- [ ] `add_extractor` 在 `on_result` 之后调用的防护——是否需要抛异常或加锁
- [ ] `extract_aggregate` 中 `draw_resources_consumed`、`draw_pity_counter_max` 等列表与 `draw_pool_ids` 长度不一致时的越界安全
- [ ] `WorkerLocalExtractor.process()` 对 `compact is None` 的处理正确性
- [ ] `StreamingSuccessCounter.get_probability()` 在 `total=0` 时的除零保护——当前返回 0.0，已验证
- [ ] `extract_process` 中 `infer_events` 的空 compact 行为——`draw_pool_ids=[]` 时
- [ ] `WorkerLocalExtractor` 的 `_kept_count` 跨多次 `process()` 调用的计数正确性
- [ ] `reset()` 方法在所有 extractor 的清理完整性

---

*待首次审计完成后更新此文件。*
