<!-- META: P22 | module:subsystems/GDR系统 | status:designing | last:2026-06-11 -->
# P22 多资源类型分布实施——手动验收清单

> 日期：2026-06-11 | 基于 P22 计划 v18 实施
> 测试基线：317 passed, 2 pre-existing fail, 0 regressions
> 修改范围：12 个核心文件 + 7 个 GUI 面板 + 1 个测试文件

---

## 一、核心层 GDR 计算正确性

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 1.1 | `parse_gdr_key` 默认回退 | `pytest tests/core/test_gdr.py -k "TestParseGdrKey" -v` | 4 个测试通过 | |
| 1.2 | `is_resource_gdr` 含 qualified key | `pytest tests/core/test_gdr.py -k "TestIsResourceGdr" -v` | 5 个测试通过 | |
| 1.3 | `get_expanded_gdr_entries` 展开正确 | `pytest tests/core/test_gdr.py -k "TestGetExpandedGdrEntries" -v` | 4 个测试通过 | |
| 1.4 | `resolve_gdr_definition` 两步查找 | `pytest tests/core/test_gdr.py -k "TestResolveGdrDefinition" -v` | 4 个测试通过 | |
| 1.5 | 多资源类型端到端计算 | `pytest tests/core/test_gdr.py -k "TestMultiResourceCompute" -v` | 11 个测试通过 | |
| 1.6 | 旧 GDR key 不崩溃 | 原有 17 个 `test_gdr.py` 测试全部通过 | 无新增失败 | |

---

## 二、统计分析面板 (`analysis_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 2.1 | GDR 下拉框条目数 | 启动 GUI → 切换到「统计分析」Tab → 查看 GDR 分布多选列表 | 17 条（无兑换货币）或 21 条（有兑换货币）；原始 `资源剩余` 不出现 | |
| 2.2 | GDR 分布默认选中 | 统计分析 → 展开 GDR 分布配置 → 查看「目标指标」下拉框 | 默认选中 `资源剩余 (抽卡资源)` | |
| 2.3 | 抽数换算后单位后缀 | 勾选「以抽数为单位」→ 查看 GDR 分布图表标题 | 资源类 GDR 标题带 `(抽)` 后缀 | |
| 2.4 | 不抽卡基线参考线 | 执行模拟 → 统计分析 → 查看「资源剩余」分布 | 绿色虚线显示不抽卡基线值 | |
| 2.5 | 条件分布基线 | 统计分析 → 条件分布 → 条件指标选资源类 | 不抽卡基线绿色虚线正确 | |
| 2.6 | 逐池山脊线图基线 | 统计分析 → 截止每池的 GDR 分布 → 山脊线图 | 不抽卡基线数值与之前一致 | |
| 2.7 | VaR/CVaR 最差/最好情形 | 统计分析 → 勾选 VaR/CVaR → 运行 | `lower_is_better` 判定正确，图表颜色不反转 | |
| 2.8 | GDR 预设阈值按钮 | 统计分析 → 条件分布 → 点击预设阈值 | 功能正常，不静默失效 | |
| 2.9 | 统计表 | 统计分析 → 勾选 GDR 指标统计 → 运行 | 表格显示正确行数，均值/中位数/标准差/VaR 列数值正确 | |
| 2.10 | 全部图表渲染 | 勾选全部图表 → 运行 | 无 HTML/JS 报错，所有图表正常显示 | |

---

## 三、过程分析面板 (`process_analysis_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 3.1 | GDR 下拉框 | 切换到「过程分析」Tab → 查看 GDR 指标下拉框 | 条目数 17/21 | |
| 3.2 | 条件 GDR 下拉框 | 过程分析 → 条件分析区域 → 查看条件 GDR 下拉框 | 同步展开 | |
| 3.3 | 事件轨迹标签 | 选取 `资源剩余 (抽卡资源)` → 查看事件轨迹列表 | GDR 显示名正确（非空白） | |
| 3.4 | 整体不崩溃 | 切换到过程分析 → 运行分析 | 无崩溃、无 ValueError | |

---

## 四、方案搜索面板 (`plan_search_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 4.1 | GDR 下拉框条目 | 切换到「方案搜索」Tab → GDR 下拉框 | 条目数 17/21 | |
| 4.2 | 不兼容 GDR 警告 | 选择 `资源消耗 (抽卡资源)` → 搜索最少资源 | 弹出不兼容提示 | |
| 4.3 | 资源分布图表 | 搜索完成 → 查看资源分布图 | X 轴标签联动资源类型 | |

---

## 五、退路分析面板 (`retreat_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 5.1 | GDR 下拉框 | 切换到「退路分析」Tab → GDR 下拉框 | 条目数 17/21 | |
| 5.2 | 山脊线图 | 运行退路分析 → 查看山脊线图 | 不抽卡基线（绿色点线）正确 | |
| 5.3 | 各池资源分布图 | 退路分析 → 各池图表 | 资源值轴正确 | |

---

## 六、策略比较面板 (`comparison_analysis_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 6.1 | GDR 下拉框 | 切换到「策略比较」Tab → GDR 下拉框 | 条目数 17/21 | |
| 6.2 | L4 散点图 X/Y 轴 | L4 帕累托散点 → 查看 X/Y 轴下拉框 | 条目数 17/21；默认 Y 轴按 `resource_efficiency:draw_resource` 匹配 | |

---

## 七、最差影响面板 (`worst_impact_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 7.1 | GDR 下拉框 | 切换到「最差影响」Tab → GDR 下拉框 | 条目数 17/21 | |
| 7.2 | 资源类 GDR + success EVT 跳过 | 选择 `资源剩余 (兑换货币)` + success 条件 → 运行 | EVT 跳过逻辑生效，不崩溃 | |
| 7.3 | 资源轴数值 | 选择不同资源类型的 GDR → 运行 | 资源值从正确字段读取 | |

---

## 八、策略优化面板 (`strategy_panel.py`)

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 8.1 | GDR 下拉框 | 切换到「策略优化」Tab → GDR 下拉框 | 条目数 17/21 | |

---

## 九、向下兼容性

| # | 验收项 | 操作步骤 | 预期结果 | ✓ |
|---|--------|---------|---------|---|
| 9.1 | 旧 key 不带 `:` | `compute_gdr_from_compact(compact, ts, 'resource_remaining')` | 值 = `final_resources['draw_resource']` | |
| 9.2 | `populate_gdr_combo(combo)` 旧签名 | 不传第二个参数 | 17 条原样输出 | |
| 9.3 | 全量测试 | `pytest tests/ -q` | 仅 2 个预先存在的失败，与本次无关 | |
| 9.4 | `draw_conversion_efficiency` 不变 | 检查 compute 函数 | 仍硬编码 `draw_resource` | |

---

## 十、已知限制（非 Bug）

| # | 限制 | 详情 |
|---|------|------|
| 10.1 | 热力图资源轴不跟随 | `streaming.py` 热力图始终显示 `draw_resource`，不受 GDR 资源类型切换影响。后续另开计划修复 |
| 10.2 | QSpinBox 保持单个 | `cost_per_draw` 输入框仅控制抽卡资源成本；兑换货币成本在对应 GDR 指标中自动使用 |
| 10.3 | `resource_efficiency` 兑换货币语义 | 公式 `achieved / total_consumed[exchange_currency]` 的解释力取决于兑换是否用于获取目标卡 |

---

## 快速验证命令

```bash
# 核心层新功能
pytest tests/core/test_gdr.py -k "TestParse or TestIsResource or TestGetExpanded or TestResolve or TestMultiResource" -v

# 全量回归
pytest tests/ -q

# 启动 GUI
python -m gacha_simulator.main
```
