# GDR 系统 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] 17 个紧凑路径 compute 函数的输入边界检查完整性
- [ ] 历史路径与紧凑路径计算结果等价性验证
- [ ] `GDRCalculator` 的 `lower_is_better` 逻辑在所有调用点一致性
- [ ] 权重参数（`desire_weights`、`miss_cost_weights`、`card_value_weights`）在各调用点的传递完整性
- [ ] `compute_gdr_from_cumulative()` 伪 compact 构造的字段完整性
- [ ] `register_gdr()` 的 overwrite 逻辑和 display_name 唯一性检查
- [ ] 资源类 GDR 硬编码 `'draw_resource'` 的影响面完整审计（P22 前需求）

---

*待首次审计完成后更新此文件。*
