# GDR 系统 -- 笔记

> 创建日期：2026-06-02

---

### 2026-06-10：P33 GDR 权重数据流重构完成

**变更摘要**：
- `SuccessChecker` → `GDRCalculator` 改名（7 文件 16 处引用）
- 新增 `make_gdr_calculator(store, target_specs, gdr_key)` 模块级工厂函数
- 删除死代码 `from_registry()`（零调用方）
- `ConfigStore` 新增 3 个权重 property：`desire_weights`、`miss_cost_weights`、`card_value_weights`
- `PlanSearchEngine` / `WorstImpactAnalyzer` 删除 3 个权重参数
- `config_panel.py` 删除 3 个 `get_xxx_weights()` getter
- 修复 10 处权重静默退化 1.0 的漏传站点
- 命名冲突解决：`gdr_analysis.py` 旧 `GDRCalculator` → `LegacyGDRCalculator`
- 全量回归：292 passed, 2 skipped

### 2026-06-11：P33 收尾——残留清理 + 归档

**补充修复**（3 个文件）：
- `retreat_search_panel.py`：删除调用已删除方法 `config_panel.get_desire_weights()` / `get_card_value_weights()` 的死代码；从 `PlanSearchEngine` 构造调用中移除三个权参
- `plan_search_panel.py`：修正 2 条过期注释（提及 `card_value_weights`）
- `retreat_panel.py`：`RetreatWorker` 权参精简——`desire_weights`/`miss_cost_weights`/`card_value_weights` 三参数合并为 `store`，Worker 内直取 `store.xxx_weights`

**归档操作**：
- P33 计划文件 + 可重构问题清单 → `docs/03-归档/plans/2026-06-10-P33-*.md`
- `模块状态矩阵.md`：P33 从 P0 移入 P2（已完成），归档路径更新
- 全量回归：292 passed, 2 skipped

**P33 后续待办（不在本次范围）**：
- `P22 多资源类型分布实施方案.md` 中 `SuccessChecker` 引用由 P22 自行更新
- `compute_vulnerability_analysis` 签名仍含三个权参——核心 API 未改，由调用方注入 store 提取的值
- `retreat_search_panel.py` 内部 `_miss_cost_weights` UI 表未删（面板未接入 UI，死代码无影响）

---

*此文件用于记录临时观察、会议纪要、设计讨论碎片等非正式内容。正式结论应提升为计划文件或更新对应的骨架文件。*
