# 策略系统 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] 7 种策略的 `select_action()` 返回值在所有状态组合下的有效性（池 ID 存在、资源充足、Action 类型正确）
- [ ] `SmartStrategy` 兑换优先逻辑的边界条件（兑换货币不足、目标卡不存在于兑换池）
- [ ] `PityReserveStrategy` 保底概率缓存在多轮调用中的正确性
- [ ] `StrategyContext` 各字段的初始化完整性——是否所有字段在所有调用路径上都正确填充
- [ ] `future_schedules` 未使用的确认——验证 7 种策略确实无任何代码路径访问此字段
- [ ] `_pity_engine`/`_pity_state` 在单进程路径（`max_workers <= 1`）下被意外修改的风险
- [ ] 所有策略的 `WAIT` 返回是否可能造成无限循环（停止条件总能终止）

---

*待首次审计完成后更新此文件。*
