# 图表引擎 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `PlotlyRenderer.build_figure()` 对所有 10 种 `ChartType` 的分发完整性——缺省分支的行为
- [ ] 中文字体在所有图表类型中的渲染一致性——`Microsoft YaHei` 在 Linux/macOS 上的回退行为
- [ ] `ChartSpec.data` 类型与 `ChartType` 的一致性验证——`chart_type="histogram"` 但 `data=ScatterData(...)` 时的行为
- [ ] `ChartWebView.set_charts()` 在 WebView 未加载完成时的 `_pending_charts` 去重/覆盖逻辑
- [ ] `runJavaScript` 的异常处理——Plotly.js 渲染失败时的错误捕获和用户反馈
- [ ] 多 WebView 实例的内存占用——每个面板一个 WebView × 10 个 Tab 时的 Chromium 进程内存
- [ ] `plotly_charts.py` 中 `_HTML_TEMPLATE` 与 `chart_webview.py` 中 `_SKELETON_HTML` 的内容同步——是否存在两份 HTML 模板的维护负担
- [ ] `ChartSpec` 便捷构造函数的默认参数与 `PlotlyRenderer` 的默认行为一致性
- [ ] 热力图（`HeatmapData`）和子图网格（`SubplotGridData`）的颜色映射（`colorscale`）在 Plotly 中的有效性验证

---

*待首次审计完成后更新此文件。*
