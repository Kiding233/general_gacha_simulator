# 停止条件 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `ResourceThresholdCondition` 的 `==` 操作符在浮点资源值下的精度安全性
- [ ] `ConsecutivePoolTargetCondition` 的池时间表遍历——在池未按结束时间排序时的行为正确性
- [ ] `CompositeStopCondition` 在 `conditions=[]`（空列表）时的行为——`any([])` 返回 `False`、`all([])` 返回 `True`
- [ ] `create_stop_condition()` 参数名与实际 `__init__` 参数名的对齐——注册表的 `params` key 与构造函数参数名的一致性
- [ ] `LastDrawCardCondition` 在 `history=[]`（空历史）时的行为——返回 `False`
- [ ] 所有条件的 `description()` 返回值的格式一致性
- [ ] `TargetAcquiredCondition` 在 `target_id` 为空字符串时的行为
- [ ] `AllPoolsEndCondition` 与 `TimeLimitCondition` 的语义重叠——两者本质相同（检查 `state.real_time >= limit`），可考虑合并

---

*待首次审计完成后更新此文件。*
