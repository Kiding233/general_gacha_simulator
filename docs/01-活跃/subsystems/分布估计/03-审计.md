# 分布估计 -- 审计

> 创建日期：2026-06-02（首次建档）
> 审计状态：首次建档，未审计

## 审计状态

本子系统于 2026-06-02 首次创建六文件骨架（00-05）。截至创建日，尚未执行正式的系统审计。以下条目留待审计时覆盖。

## 待审计维度

### 正确性

- [ ] `EmpiricalDistribution.quantile()` 经验分位数线性插值与 numpy `np.quantile()` 的对齐性
- [ ] `_empirical_cvar()` 分数权重计入的正确性（2026-05-26 已修复缺陷 1：CVaR ceil 偏差）
- [ ] 下尾取负法 `fit_gpd_lower()` 的 VaR_X = -VaR_Y 符号正确性
- [ ] `evt_var_right()` 中 ξ≈0 极限与 ξ≠0 通式的数值一致性边界
- [ ] 有界支撑（ξ < 0）的端点约束 `var >= endpoint` 在浮点精度下的稳定性
- [ ] `JointSamples.conditional_second_below_quantile()` 阈值比较符号的断言（≤ 还是 <）

### 全面性

- [ ] 所有 13 种注册 GDR 是否都有对应的 `EmpiricalDistribution` 覆盖路径
- [ ] `WorstImpactAnalyzer` 条件成功/失败子集跳过 EVT 的完整性
- [ ] 边界输入处理：空列表、全相同值、含 NaN/Inf、含 None 的混合类型序列
- [ ] `conditional_bottom_alpha()` 和 `conditional_top_alpha()` 的 `cutoff=0` → `cutoff=1` 的保底语义

### 鲁棒性

- [ ] GPD 拟合失败的全部回退路径是否都被覆盖（样本不足 / MLE 失败 / 正则性不满足 / 外推越界）
- [ ] `_count_distinct()` 的惰性计算 + 缓存在并发读取下的安全性（当前 GIL 保护，未来 asyncio 需加锁）
- [ ] `freedman_diaconis_bins()` 对 IQR=0（全相同值）、n=1、n=2 的边界行为
- [ ] scipy 版本兼容性：`genpareto.fit(floc=0)` 在 scipy 1.5+ 可用，1.4- 行为未验证

### 一致性

- [ ] `EmpiricalDistribution._evt_quantile()` 和 `BootstrapEngine._bootstrap_tail_gpd()` 的 GPD 拟合路径一致性（当前不一致——两套独立实现）
- [ ] `WorstCaseAnalysis.cvar()` 和 `BestCaseAnalysis.conditional_top()` 的 α 对称性
- [ ] `distribution.py` 和 `risk_analysis.py` 中重复的 WorstCase/BestCase 分析逻辑

### 深度

- [ ] 是否有单元测试覆盖所有退化和边界输入？
- [ ] 是否有测试验证 GPD 拟合的 ξ 和 β 与已知厚尾分布（Pareto）的理论值接近？
- [ ] 是否有 monotonic 测试验证 `quantile(p)` 对 p 单调非减？
- [ ] 是否有测试验证 `cdf(quantile(p)) ≈ p` 在容差范围内？

## 审计历史

| 日期 | 审计人 | 覆盖范围 | 发现问题 | 状态 |
|------|--------|---------|---------|------|
| 2026-06-02 | — | 首次建档，未审计 | — | — |
