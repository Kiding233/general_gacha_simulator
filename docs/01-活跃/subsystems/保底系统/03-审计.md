# 保底系统 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `get_probabilities()` 纯查询属性验证——确认所有代码路径均不修改 `PityState`
- [ ] `before_draw()` 的 `increment` 在异常路径（池不存在、保底未定义）下的行为一致性
- [ ] `after_draw()` 的重置条件判定正确性——`any_ssr`/`featured_ssr`/`never` 在所有保底组合下
- [ ] 多保底管道叠加后的概率分布归一化验证——`sum(probs) == 1.0` 在所有保底组合下
- [ ] `SoftPityBehavior._apply_targeted()` 在 `target_prob >= 1.0` 时的边界处理
- [ ] `HardPityBehavior.apply()` 在 `resolved_targets` 含不存在于 `probabilities` 的 card_id 时的行为
- [ ] `parse_pity_file()` 的异常输入容错（格式错误、数值越界、空的 pools 字段）
- [ ] 保底管道中的 `resolved_targets` 传递完整性——所有保底类型均正确传递
- [ ] `PityState.reset()` 的重置后计数器值（应为 0，而非 key 删除）在下游代码中的语义一致性

---

*待首次审计完成后更新此文件。*
