# 脆弱性分析面板 — 审计

> 建档日期：2026-06-08 | 版本：v2.0（P8 面板合并后重写）

---

## 一、设计 ↔ 实施对齐检查

| 设计项 | 实施状态 | 备注 |
|--------|---------|------|
| LLR + 逻辑回归双路径 | ✅ | 逻辑回归为主，LLR 为方差估计 + 回退 |
| r_grid 扩展超出数据范围 | ✅ | margin = 10% span，自动计算 |
| 左边界偏差修复 | ✅ | 带宽扩展 + 核权重非对称 |
| 高斯核平滑方差估计 | ✅ | 向量化实现 |
| 脆弱区间连续提取 | ✅ | 逐点阈值 → 连续区间 |
| 全局统一分箱（山脊图 + 单池图） | ✅ | `compute_bins()` → `global_bin_edges` |
| 保底统计快照 | ✅ | 仅收集失败且在脆弱区间内的模拟 |
| Plotly 图表渲染 | ✅ | ChartWebView（非 matplotlib） |
| vulnerability_finished 信号传递给 PlanSearchPanel | ✅ | MainWindow._connect_signals() |
| 错误处理 | ✅ | `try/except` + `traceback.print_exc()` |
| P8 面板合并——拆分为独立一级 Tab | ✅ | 退路搜索逻辑迁至 PlanSearchPanel |

---

## 二、测试覆盖

| 测试范围 | 状态 | 文件 |
|----------|------|------|
| VulnerabilityInterval 构造 | 待补充 | — |
| PoolVulnerabilityResult 序列化 | 待补充 | — |
| `compute_vulnerability_analysis()` 空输入 | 待补充 | — |
| `compute_vulnerability_analysis()` 正常输入 | 待补充 | — |
| `_logistic_ll_fit()` 二值响应 | 待补充 | — |
| `_ll_fit()` 连续响应 | 待补充 | — |
| `_kernel_smooth_variance()` 边界行为 | 待补充 | — |
| `_find_contiguous_intervals_from_curve()` | 待补充 | — |
| 分箱一致性（global_bin_edges 传递） | 待补充 | — |

> **注意**：脆弱性分析模块目前没有独立的单元测试文件。以上所有项目标记为「待补充」。建议在 P15 阶段二实施时同步补齐核心测试。

---

## 三、P8 合并审计（2026-06-08）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| RetreatPanel 代码未修改 | ✅ | P8 仅修改 MainWindow 集成方式，面板代码未动 |
| vulnerability_finished 信号链路 | ✅ | MainWindow._connect_signals() 连接 |
| 旧 `retreat_search_panel.py` 已 deprecated | ✅ | 文件顶部标注 |
| 目录从 `退路分析/` → `脆弱性分析/` | ✅ | P8 Phase F 完成 |
| P26 计划已迁至 `方案搜索/` | ✅ | P8 Phase F 完成 |
| 六文件已重写（v1.0 → v2.0） | ✅ | 本次审计触发 |

---

## 四、已知未对齐项

| 项目 | 偏差 | 严重度 | 建议 |
|------|------|--------|------|
| 类名 `RetreatWorker` / `RetreatPanel` | 文件名和类名仍含 "Retreat"，与 Tab 名「脆弱性分析」不一致 | 低 | 下一轮重构时改为 `VulnerabilityWorker` / `VulnerabilityPanel` |
| 无独立单元测试 | 脆弱性分析管线无自动化测试覆盖 | 中 | P15 阶段二同步补齐 |
| `_extract_cost_per_draw()` 重复 | retreat_panel.py 与 plan_search_panel.py 各有独立实现 | 低 | 应统一使用 `core/retreat_search.py` 的 `get_cost_per_draw()` |

---

*审计记录完成。*
