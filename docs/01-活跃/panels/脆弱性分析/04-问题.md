# 脆弱性分析面板 — 待解决问题

> 建档日期：2026-06-08 | 版本：v2.0（P8 面板合并后重写）
> 问题来源：P15（脆弱性分析 9 项缺陷，阶段一完成 / 阶段二/三待做）
> P26 相关条目已迁至 `方案搜索/04-问题.md`

---

## 优先级体系

| 优先级 | 含义 | 建议时间窗口 |
|--------|------|-------------|
| P0 | 阻塞性——影响结论正确性或用户体验显著受损 | 2-3 天 |
| P1 | 重要——提升统计严谨性，建议近期完成 | 3-7 天 |
| P2 | 增强——锦上添花，可择机实施或搁置 | 后续评估 |

---

## P1：P15 阶段二 —— 统计严谨性提升

### P1-A：RSC 带宽选择（缺陷 D）

| 属性 | 内容 |
|------|------|
| 涉及文件 | `core/vulnerability.py` |
| 预计工作量 | 1-2 天 |
| 依赖 | 无（独立于其他修改） |
| 风险 | 中——RSC 的 O(n²) 留一估计在 n>5000 时需降级为 GCV 近似 |
| 文献 | Fan & Gijbels (1995) 残差平方准则 |

### P1-B：Wild Bootstrap 同时置信带（缺陷 E）

| 属性 | 内容 |
|------|------|
| 涉及文件 | `core/vulnerability.py` |
| 预计工作量 | 2-3 天 |
| 依赖 | 局部 logistic 回归（阶段一已完成）效果最好 |
| 风险 | 中——B=999 × IRLS 迭代的耗时，需提供 `fast=True` 渐近近似模式 |
| 文献 | Härdle & Mammen (1991); Wasserman (2006) Ch.5 |
| 备注 | 与 P3（Bootstrap 稳定性分析）互补但不重叠 |

### P1-C：persistence 过滤（缺陷 F）

| 属性 | 内容 |
|------|------|
| 涉及文件 | `core/vulnerability.py` |
| 预计工作量 | 0.5 天 |
| 依赖 | 最好在 Wild Bootstrap（P1-B）之后，可用同时置信带而非逐点阈值 |
| 风险 | 低——仅增加 `min_interval_width` 参数和连续格点计数 |

---

## P2：P15 阶段三 —— 完善与优化

### P2-A：KNN 自适应带宽（缺陷 G）

| 属性 | 内容 |
|------|------|
| 涉及文件 | `core/vulnerability.py` |
| 预计工作量 | 1 天 |
| 依赖 | 无 |
| 风险 | 低——作为可选参数 (`bandwidth_method='knn'`)，默认保持 Silverman |

### P2-B：UI 因果性注记（缺陷 I）

| 属性 | 内容 |
|------|------|
| 涉及文件 | `gui/retreat_panel.py` |
| 预计工作量 | 0.5 天 |
| 依赖 | 无 |
| 风险 | 极低——仅增加一行灰色注记文字说明关联性 |

---

## 已解决

| # | 问题 | 解决方案 | 完成日期 |
|---|------|---------|---------|
| A | r_grid 边界硬限制 | r_grid 扩展为 `[r_min−margin, r_max+margin]` | 2026-05-26 |
| B | LLR 左边界系统性偏差 | 左边界带宽扩展 + 核权重非对称 | 2026-05-26 |
| C | 二元响应线性回归 | 局部逻辑回归 IRLS 实现 | 2026-05-26 |
| - | P8 面板合并——拆分双面板架构 | RetreatPanel 独立为一级 Tab「脆弱性分析」 | 2026-06-08 |
| - | 六文件过时（描述旧双面板） | 六文件 v2.0 重写 | 2026-06-08 |

---

## 依赖关系图

```
P15 阶段一 (A/B/C) ✅
       │
       ├─→ P1-A (RSC带宽) ──── 独立
       ├─→ P1-B (Wild Bootstrap) ──── 受益于阶段一的 local logistic
       │       │
       │       └─→ P1-C (persistence过滤)
       │
       ├─→ P2-A (KNN带宽) ──── 独立
       └─→ P2-B (因果性注记) ── 独立
```

---

## 与外部计划的交叉

| 外部计划 | 与本模块的关系 |
|----------|---------------|
| P3（Bootstrap 引擎） | P1-B（Wild Bootstrap）可复用 P3 的重抽样基础设施 |
| P4（自适应模拟） | 自适应模拟可产生更多低资源区域样本，间接改善边界估计 |
| P8（面板合并） | ✅ 已完成——P8 拆分了双面板架构，本模块不再包含退路搜索逻辑 |

---

## 追踪状态

| 问题编号 | 关联计划 | 状态 | 开始日期 | 完成日期 |
|---------|---------|------|---------|---------|
| P1-A (RSC) | P15 阶段二 | 未开始 | — | — |
| P1-B (Wild Bootstrap) | P15 阶段二 | 未开始 | — | — |
| P1-C (persistence) | P15 阶段二 | 未开始 | — | — |
| P2-A (KNN) | P15 阶段三 | 未开始 | — | — |
| P2-B (因果性) | P15 阶段三 | 未开始 | — | — |

---

*问题追踪文档完成。*
