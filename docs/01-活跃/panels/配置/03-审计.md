# 配置面板 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `ConfigStore` 所有字段的序列化/反序列化往返一致性（`save → load → save` 完全等价）
- [ ] 竖线分隔文本解析器（`config_io.py` 各 `_load_xxx` 函数）对异常输入（空行、多余分隔符、编码错误）的鲁棒性
- [ ] `_bonus_to_text()` / `_parse_bonus_text()` 往返一致性——额外资源奖励的紧凑文本格式
- [ ] 各子标签的「空状态」处理——无池子、无保底、无目标卡时 UI 不崩溃
- [ ] `ConfigPanel._refresh_all()` 的性能——2927 行代码在刷新大量条目（如 >100 张卡牌）时是否阻塞 UI 线程
- [ ] 策略参数编辑控件与 `strategy_params` dict 的键一致性和类型安全
- [ ] 权重配置的三级结构（disire/miss_cost/card_value）是否正确区分目标卡与非目标卡
- [ ] `set_store()` 多次调用（如加载 → 编辑 → 重新加载）不会产生残留数据

---

*待首次审计完成后更新此文件。*
