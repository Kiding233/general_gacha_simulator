# 策略比较 — 审计

> 建档日期：2026-06-02 | 更新：2026-06-04 | 版本：v2

---

## 2026-06-04：P19 Phase 1+2 完成后审计

### 架构对齐

| 检查项 | 状态 |
|--------|------|
| 模拟与分析分离 | ✅ ResultStore 集中管理，ComparisonAnalysisPanel 从 ResultStore 读取 |
| 数据持久化 | ✅ JSON 持久化 + 导入/导出 |
| GDR 框架统一 | ✅ compute_gdr_values_for_datasets() 使用 13 种 GDR 指标 |
| 统计检验 | ✅ L2 DD Bootstrap + L3 KS/MWU/ttest + Holm-BH 校正 |
| 可比性检查 | ✅ 9 维度 ComparabilityFingerprint + 4 种比较模式 |
| 图表渲染 | ✅ Plotly ECDF/PMF/帕累托 (ChartWebView) |

### 测试完整性

| 检查项 | 状态 |
|--------|------|
| 现有 286 测试 | ✅ 全部通过，无回归 |
| 新模块测试 | ⚠ 无专有测试文件（result_store / comparison_analyzer 无 test_*.py） |

### 代码审查记录

- `038ba30`: result_store — 全对比较 + rename 信号 + 移除未使用 import（两阶段审查：spec → code quality）
- Phase 2 面板文件未经历正式代码审查

### 已知审计风险

1. **ComparisonAnalyzer 无单元测试**：`dd_bootstrap_test` / `compute_pvalue_matrix` / `holm_bonferroni` / `benjamini_hochberg` 无测试覆盖
2. **ComparabilityFingerprint 的 config_hash** 使用 `hashlib.sha256`，依赖 `Pool.__hash__` 等隐式行为
3. **ComparisonWorker 无超时/取消机制**：后台线程启动后无法提前终止

---

## 首次建档 (2026-06-02)

首次建档，未审计。
