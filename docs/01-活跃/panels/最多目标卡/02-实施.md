# 最多目标卡面板 -- 实施

> 创建日期：2026-06-02（首次建档）
> 关联模块：`gui/strategy_panel.py`（672 行）、`core/forward_backward.py`
> 关联计划：P8 面板合并（将整合到 PlanSearchPanel）

---

## 一、代码位置

### 主文件：`gacha_simulator/gui/strategy_panel.py`

**行数**：672 行

**内容分布**：

| 行号范围 | 内容 |
|----------|------|
| 1-27 | 导入（PyQt6 + 内部模块） |
| 28-63 | `StrategyWorker.__init__()` — 接收搜索参数 |
| 64-69 | `StrategyWorker.stop()` — 安全中止 |
| 70-133 | `StrategyWorker._forward_method()` — 前进法主循环 |
| 135-250+ | `StrategyWorker._backward_method()` — 后退法主循环 |
| 250+ | `StrategyPanel` 类定义 — UI 布局、信号连接 |

### 数据结构：`gacha_simulator/core/forward_backward.py`

```python
@dataclass
class ForwardStep:
    added_card_id: str
    target_set: Set[str]
    success_probability: float
    target_specs: Dict[str, int]

@dataclass
class ForwardResult:
    steps: List[ForwardStep]
    final_target_set: Set[str]
    final_success_probability: float
    final_target_specs: Dict[str, int]

@dataclass
class BackwardStep:
    removed_card_id: str
    target_set: Set[str]
    success_probability: float
    target_specs: Dict[str, int]

@dataclass
class BackwardResult:
    steps: List[BackwardStep]
    final_target_set: Set[str]
    final_success_probability: float
    final_target_specs: Dict[str, int]
```

同样包含 `ResourceSearchStep` / `ResourceSearchResult`（资源搜索面板使用）。

---

## 二、StrategyPanel UI 布局

| 控件 | 类型 | 说明 |
|------|------|------|
| 搜索方法 | QComboBox | 前进法 / 后退法 |
| GDR 指标 | QComboBox | 17 种 GDR 可选，通过 `populate_gdr_combo()` 填充 |
| 成功阈值 | QDoubleSpinBox | 默认 0.8（80% 成功率） |
| 目标数量 | QSpinBox | 每张目标卡的需求数量（默认 1） |
| 模拟次数/迭代 | QSpinBox | 每轮搜索迭代的模拟次数 |
| 并行进程数 | QSpinBox | 传递给 `run_batch_parallel()` |
| 停止按钮 | QPushButton | 中场停止搜索（设置 `_should_stop` 标志） |
| 权重表 | QTableWidget | 三列：desire / miss_cost / card_value |
| 进度条 | QProgressBar | 数值进度 |
| 图表区 | ChartWebView | Plotly 渲染的搜索过程图 |

---

## 三、StrategyWorker 关键逻辑

### 前进法流程

```
1. 按 desire 权重降序排列候选卡
2. 初始化 current_set = {}, last_valid_set = {}
3. for card_id in sorted_ids:
     a. current_set.add(card_id)
     b. run_batch_parallel(N次模拟, target_specs=current_specs)
     c. compute_success_probability() → prob
     d. 记录 ForwardStep
     e. if prob >= threshold:
          last_valid_set = current_set (更新最优)
        else:
          break (停止)
4. 返回 ForwardResult(steps, last_valid_set, last_valid_prob)
```

### 后退法流程

```
1. 按 miss_cost 权重升序排列候选卡
2. 初始化 current_set = 全部候选卡
3. 先评估全集成功率 initial_prob
4. if initial_prob >= threshold: 直接返回（无需移除）
5. for card_id in sorted_ids:
     a. current_set.remove(card_id)
     b. run_batch_parallel(N次模拟, target_specs=current_specs)
     c. compute_success_probability() → prob
     d. 记录 BackwardStep
     e. if prob >= threshold:
          last_valid_set = current_set
          break (停止)
6. 返回 BackwardResult(steps, last_valid_set, last_valid_prob)
```

---

## 四、关键依赖

| 依赖 | 方向 | 说明 |
|------|------|------|
| `core/forward_backward.py` | 被调用 | 搜索步骤和结果的数据结构 |
| `core/gdr.py` → `compute_success_probability()` | 被调用 | 成功率评估 |
| `gui/batch_simulator.py` | 被调用 | `SimulationEnvBuilder` + `run_batch_parallel()` |
| `gui/chart_webview.py` | 被调用 | Plotly 图表显示 |

---

*实施文档覆盖代码位置、UI 布局和两种搜索算法的伪代码。*
