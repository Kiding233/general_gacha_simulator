# 过程分析面板 -- 笔记

> 创建日期：2026-06-02

---

### 2026-06-02：SVG 两层事件分类体系集成

从 `架构设计.md` §1.8 将理论参考.svg 的两层事件分类体系（单抽层 8 种 × 池子层 12 种）拆入过程分析模块各 md 文件：

- `00-档案.md`：设计意图中新增「设计愿景」框——当前 7 种事件为简化实现，两层体系为未来细化方向
- `01-理论.md`：新增 §1.5（两层分类体系的完整决策树 + 代码/SVG 对照 + 两层交叉的理论可能性），扩展 §2.3 为更全面的「事件类型的语义合并与信息损失」（含 5 个合并点逐一分析 + 缺失维度表），新增 §2.4（两层交叉的设计挑战——多 SSR 汇总的开放性设计问题）+ §2.5（转移矩阵分析缺失）
- `02-实施.md`：新增「五、当前实现与 SVG 设计的差距」表（7 项差距——含所需数据可用性评估）
- `04-问题.md`：登记 P34（单抽层细化）、P35（池子层细化）、P36（转移矩阵实现），标注架构设计关联条目 C3/C6/C10
- `05-笔记.md`：本条记录

**关键判断**：
- 单抽层细化（区分保底出 vs 歪得、歪非目标 up vs 歪常驻）所需数据已在 `_infer_from_draw_sequence()` 中可用——是**纯分类逻辑改动**，非新数据采集
- 池子层「全部/部分」维度同样无需新数据（`target_ids` + `card_counts` 已可判定）
- 池子层「免费/非免费」和「浪费资源」维度依赖上游改动（`CompactResult` 新字段 / `ConfigStore.targets` 需求数量）
- 两层交叉（C10）是**开放性设计问题**——多 SSR 汇总规则尚未定义，不宜作为简单实现任务
- 转移矩阵（C9）应优先于事件细化（P34/P35）——它用现有数据即可实现，能立即提供分析价值

---

*此文件用于记录临时观察、会议纪要、设计讨论碎片等非正式内容。正式结论应提升为计划文件或更新对应的骨架文件。*
