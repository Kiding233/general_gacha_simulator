# 过程分析面板 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `infer_events()` 两种推断路径（序列 vs 聚合）对同一 CompactResult 产生的事件类型一致性
- [ ] 7 种事件类型的互斥完备性——所有（池类型 × 抽卡次数 × 命中 × 保底）组合是否均被覆盖
- [ ] `_resolve_skip_ignore()` 在 `pool_target_map` 为空时的回退启发式（`has_target_in_pool or target_ids`）是否合理
- [ ] Laplace 平滑的 K 值选择——是否始终等于事件类型数量（含细化后的 `pity_hit:{name}` 子类型）
- [ ] `low_sample` 阈值（默认 30）在高度偏斜概率（p<0.05）下的实际 Type I/II 错误率
- [ ] 自定义约束模式（custom）的 `any` 操作符——跳过该事件类型时，其余约束的组合是否完整
- [ ] 资源池事件的跳过逻辑（`pool_type == 'resource'` 且 `continue`）——是否会导致某些模拟在过程分析中无事件记录
- [ ] **SVG 两层对齐**：当前 7 种事件到 SVG 单抽层 8 种 + 池子层 12 种的映射是否忠实（见 `01-理论.md` §1.5 对照表）——特别检查 `pity_hit` 对「保底歪得目标卡」的合并是否在 `_infer_from_draw_sequence()` 中有条件分歧
- [ ] **细化数据可用性**：`_infer_from_draw_sequence()` 逐抽遍历中，每抽的 `pity_triggered`/`card_id`/`is_target` 是否均已捕获——验证事件细化（P34）无需新数据采集的断言（见 `05-笔记.md` 2026-06-02）
- [ ] **pool_target_map 完整性**：是否能正确区分 SVG 池子层的「计划内」（up 含未抽出目标卡）vs「计划外」（up 不含未抽出目标卡）——需验证「已抽出目标卡从 map 中移除」的逻辑是否存在

---

*待首次审计完成后更新此文件。*
