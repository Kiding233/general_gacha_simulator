# 批量模拟面板 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `SimulationThread.run()` 异常处理完整性——`GachaService` 崩溃、`multiprocessing.Pool` fork 失败、OOM 等异常路径
- [ ] `no_draw` 基线计算在 `max_workers=1` 单进程路径下的全局变量污染风险（参见 §2.2）
- [ ] `_batched_progress` 闭包在 N 极小时（如 N=1）不丢失最终进度 emit
- [ ] `result_bundle` 中的 `extraction` 可能为 None 的降级路径（单进程且无 extractor）
- [ ] `run_batch_parallel()` 的 `seed` 参数传递——每个子进程的种子是否唯一且无碰撞
- [ ] `SimulationThread` 被提前终止（如用户关闭窗口）时，`multiprocessing.Pool` 是否正确清理（避免僵尸进程）
- [ ] 并行进程数超过 CPU 核心数时性能退化程度（预期线性退化，但需验证无死锁）

---

*待首次审计完成后更新此文件。*
