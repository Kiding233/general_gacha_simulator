# 手动验收项目清单

**日期**：2026-05-26 | **状态**：待验收

> **规则**：未实施/未验收的项目始终在前，已实施/已验收的项目挪到最后。

---

## 本轮新增 — Plotly 图表迁移验收（S1~S3.2）

> 将 matplotlib Agg → 临时 PNG → QPixmap → QLabel 管线替换为 Plotly + QWebEngineView，获得交互式图表、HiDPI 原生支持、消除临时文件管理。

### 基础架构验证

- [ ] **ChartWebView 正确加载 plotly.js**：启动 GUI → 打开任意已迁移面板 → 图表区域显示交互式 Plotly 图表（非静态图片），终端输出 `[ChartWebView] plotly==x.y.z` 版本日志
- [ ] **plotly.js 本地离线加载**：断网后启动 GUI → 已迁移面板的图表仍正常渲染（不依赖 CDN）
- [ ] **HiDPI 渲染清晰**：在高 DPI 显示器（≥150% 缩放）上查看图表 → 文字和线条清晰，无像素锯齿或模糊
- [ ] **AA_ShareOpenGLContexts 已设置**：启动 GUI 不报 `QtWebEngineWidgets must be imported before QCoreApplication` 错误
- [ ] **临时文件管理**：运行各面板分析后，检查系统临时目录 → `chart_` 前缀的临时目录存在，关闭面板/退出应用后自动清理

### 交互功能验证

- [ ] **悬停数据提示**：鼠标悬停在图表数据点上 → 弹出浮层显示具体数值（Plotly 默认行为）
- [ ] **框选缩放**：在图表上拖拽矩形区域 → 图表放大到选中区域
- [ ] **双击重置**：双击图表 → 缩放恢复默认
- [ ] **工具栏保存为 PNG**：点击图表工具栏「下载为 PNG」按钮 → 成功导出 PNG 图片
- [ ] **工具栏其他功能**：缩放、平移、框选、套索等工具栏按钮均可正常使用

### 各面板逐一验收

#### worst_impact_panel（最差影响）

- [ ] **条形图渲染**：运行最差影响分析 → 显示交互式条形图，每项一个条形，悬停显示数值
- [ ] **期望值参考线**：条形图上有期望值竖线标注（红色虚线）
- [ ] **表格+图表共存**：上方 QTableWidget（最差影响数据表）+ 下方 ChartWebView（分布条形图），布局正常
- [ ] **导出按钮正常**：「导出CSV」按钮功能不受影响

#### process_analysis_panel（过程分析）

- [ ] **条件分布直方图**：运行过程分析 → 条件分布图显示为交互式直方图，有均值线和分位数线
- [ ] **样本量不足提示**：样本量不足时，ChartWebView 中显示文字提示（非白屏）
- [ ] **切换事件/成败模式**：切换不同事件类型和成败组合 → 图表正确更新

#### resource_search_panel（最少资源）

- [ ] **多轨迹散点图**：运行资源搜索 → 图表显示三组散点（搜索上界/二分搜索/最终验证），颜色和标记形状不同
- [ ] **阈值参考线**：图表中出现资源阈值水平线和最小值竖线
- [ ] **图例正确**：图例显示三组轨迹的名称，颜色与图表一致

#### strategy_panel（策略优化）

- [ ] **折线图渲染**：运行策略扫描 → 显示折线图（lines+markers），数据点可悬停查看
- [ ] **最优值参考线**：图表中有最优策略的水平参考线
- [ ] **多策略对比**：多个策略的曲线在同一图表中对比

#### retreat_search_panel（退路搜索）

- [ ] **帕累托散点图**：运行退路搜索 → 显示着色散点图（RdYlGn 色阶），颜色深浅对应不同资源水平
- [ ] **色阶图例**：图表右侧显示色阶 colorbar
- [ ] **坐标轴标签**：X 轴为资源剩余，Y 轴为成功率

#### retreat_panel（退路分析）

- [ ] **JS 标签切换**：运行脆弱性分析 → 图表区域顶部显示 JS 标签栏（「总览」+ 各池子名称），点击标签瞬时切换图表，无闪烁
- [ ] **总览标签**：显示山脊线图（ridge plot），每个池子一条分布曲线，绿色虚线标记不抽卡基线
- [ ] **各池子标签**：每个池子标签显示直方图（分布）+ 散点图（条件概率曲线，如有），图表间用 HTML 折叠面板分隔
- [ ] **多标签无重载**：标签切换仅为 CSS `display: block/none`，不重新加载 HTML 页面
- [ ] **删除旧 QTabWidget**：不再出现 Qt 原生标签页组件，所有标签切换由 HTML/CSS/JS 处理
- [ ] **信息文字保留**：每个池子的统计信息（到达模拟数/失败数/失败率/脆弱区间）仍正常显示

#### analysis_panel（统计分析）

- [ ] **GDR 分布图（直方图）**：勾选 GDR 分布 → 选择一个 GDR 指标选「分布」→ 运行分析 → 显示交互式直方图，有均值线和 VaR 分位数线
- [ ] **GDR 分布图（累积分布）**：勾选「累积分布」→ 运行 → 显示 CDF 曲线图
- [ ] **不抽卡基线参考线**：选择「资源剩余」的分布/CDF 图表 → 图表中出现绿色虚线标记不抽卡基线
- [ ] **最差情形分析图**：运行风险分析 → 显示双层直方图（全部数据 + 尾部数据叠加，不同颜色），有 VaR 分界线
- [ ] **条件分布图**：运行条件分布分析 → 显示三层直方图（全部 + 成功条件 + 失败条件，绿/红色叠加）
- [ ] **时间序列图**：运行时间演化 → 显示多条折线（每条一个模拟样本路径）
- [ ] **热力图**：运行时间热力图 → 3 个子图（目标达成率/资源剩余/SSR出数），各自独立渲染
- [ ] **3D 瀑布图**：运行 3D 瀑布图 → 显示交互式 3D 散点图（可旋转/缩放）
- [ ] **每池分析条形图**：运行每池分析 → 显示水平条形图（每池平均抽卡数/目标卡数/保底数）
- [ ] **截止每池分布（山脊线图）**：运行截止每池的 GDR 分布 → 显示山脊线图（每个池子一条分布曲线）
- [ ] **抽卡数-GDR 散点图**：显示散点图
- [ ] **相关性热力图**：显示热力图矩阵，含相关系数标注
- [ ] **转变分析图**：显示成功率变化折线图 + 每对池子的转移矩阵热力图
- [ ] **折叠面板展开/折叠**：图表使用 HTML 折叠面板（collapsible sections），点击标题可展开/折叠
- [ ] **增量分析不报错**：首次运行分析（选部分图表）→ 增加勾选新图表 → 再次运行 → 浏览器控制台无 `Chart div not found` 错误
- [ ] **图表高度充足**：统计分析面板的图表区域高度 ≥ 800px，单张图表不需要滚动即可看全
- [ ] **表格+图表混合布局**：表格（GDR 统计/VaR-CVaR 等）在上方，ChartWebView 在下方，布局合理
- [ ] **清除结果**：点击「清除结果」→ 表格行删除、ChartWebView 隐藏、提示文字恢复

### Bug 修复记录

- [x] **增量分析 JS 报错修复**（2026-05-26）：新选图表 key 不存在于 HTML 中时，自动合并到缓存并全量 `set_charts()` 重建，避免 `Chart div not found` 错误
- [x] **图表高度修复**（2026-05-26）：ChartWebView 最小高度从 300px 提升到 800px + Expanding 尺寸策略

#### vulnerability.py 独立测试

- [ ] **plot_vulnerability_ridge 返回 ChartSpec**：单测验证返回 `ChartSpec(chart_type="ridge")` 而非文件路径
- [ ] **plot_vulnerability 返回 dict**：单测验证返回 `{"histogram": ChartSpec, "curve": ChartSpec|None, "info_text": str}`
- [ ] **63 个测试全部通过**：`pytest tests/visualization/ tests/core/test_vulnerability.py -v` 全部通过

### 回归检查

- [ ] **批量模拟功能正常**：启动 GUI → 批量模拟 Tab → 运行模拟 → 结果正确
- [ ] **未迁移面板不受影响**：analysis_panel（统计分析）仍使用 matplotlib 渲染，功能正常
- [ ] **应用启动无 QSS 解析错误**：控制台无警告
- [ ] **所有面板的「运行」按钮在分析完成后恢复可点击状态**
- [ ] **进度条正常更新**：分析过程中进度条从 0% 走到 100%

---

## P4 — GDR框架 lower_is_better

- [ ] **(-)GDR 指标前缀**：打开统计分析面板 → GDR指标下拉框 → `resource_per_card`、`resource_consumed`、`draw_conversion_efficiency` 显示名前带 `(-)` 前缀
- [ ] **lower_is_better 成功判断**：选择 `(-)每目标卡资源消耗` → 阈值设为 100 → 运行分析 → 单次模拟消耗 ≤100 时应判定为成功
- [ ] **默认 GDR 无前缀**：`target_achievement`、`all_targets` 等指标显示名不带 `(-)` 前缀

---

## P5 — 新增 (-)GDR

- [ ] **每目标卡资源消耗**：运行分析后选择该指标，数值 = 总消耗 / 获得的目标卡数量（截断到需求量）
- [ ] **资源消耗**：运行分析后选择该指标，数值 = 总资源消耗量
- [ ] **未获得目标卡时**：`每目标卡资源消耗` 显示为 inf（无穷大）

---

## P9 — 卡牌初始持有数量

- [ ] **UI 列**：配置面板 → 卡牌定义表 → 第5列「初始持有」为 QSpinBox（范围 0-9999，默认 0）
- [ ] **提示文字**：策略Tab 目标卡区域下方出现灰色提示文字「提示：「需求数量」表示还需从抽卡中获得的额外数量...」
- [ ] **加载旧配置**：加载不含 `initial_count` 的旧 cards.txt 时，所有卡牌初始持有默认为 0，不报错
- [ ] **保存配置**：含 `initial_count > 0` 的卡牌保存到 cards.txt 第四列，为 0 的不写入
- [ ] **初始持有不满足目标**：设某卡初始持有 5 张，需求 2 张 → 运行模拟 → 仍需从 gacha 中抽到 2 张（card_counts 从 0 开始，不含初始持有）
- [ ] **初始持有 + 额外抽到**：设某卡初始持有 1 张，需求 3 张 → 运行模拟 → card_counts 从 0 开始累加新抽到的，抽到 3 张后满足
- [ ] **card_counts 不含初始持有**：运行模拟后查看结果，card_counts 只反映从 gacha 中抽到的数量，不含初始持有

---

## P10 — 第N次/超出N张额外资源

- [ ] **分布对话框「额外资源」列**：双击池配置的分布编辑 → 对话框有第6列「额外资源」，QLineEdit，placeholder 显示格式提示
- [ ] **first_time_bonus 编辑**：输入 `ft:gem:50` → 保存 → 该卡首次获得时额外获得 50 gem（总持有 = initial_count + 新抽到 = 1 时触发）
- [ ] **nth_time_bonus 编辑**：输入 `nth:3=gem:100;5=gem:200` → 总持有（initial_count + 新抽到）达第3次额外100gem，第5次额外200gem
- [ ] **excess_bonus 编辑**：输入 `xs:5>starlight:25` → 总持有超过5张后再获得时额外获得25 starlight
- [ ] **额外资源语法**：分布对话框第6列 placeholder 显示 `ft:r:a & nth:n=r:a;n=r:a & xs:t>r:a`（用 `&` 分隔，用 `;` 分隔多个 nth）
- [ ] **首次获得**：initial_count=0 且首次从 gacha 抽到时，`first_time_bonus` 生效；若 initial_count>0 则不触发（已拥有）
- [ ] **第N次获得**：总持有计数（= initial_count + 新抽到数）达到 N 时触发 `nth_time_bonus`
- [ ] **超出阈值**：总持有超过 `excess_bonus` 阈值的每次获得都触发额外资源
- [ ] **保存加载**：分布对话框中编辑额外资源 → 保存配置 → 重新加载 → 额外资源列内容保留

---

## P11 — 转变分析统一

- [ ] **转变分析正常运行**：过程分析面板 → 勾选「转变分析」→ 运行 → 不报错、不跳过
- [ ] **三种成功标准**：切换成功标准（全部目标/任意SSR/每池目标），转变矩阵数据相应变化
- [ ] **无数据时警告**：无池结束时间或无累积快照时运行转变分析 → 显示警告信息而非静默失败
- [ ] **旧模式兼容**：旧的成功标准选择（all_targets/any_ssr/per_pool_target）通过 GDR 路径计算

---

## P12 — 策略参数动态控件

- [ ] **切换策略时参数变化**：在策略Tab切换不同策略类型 → 参数面板控件动态变化（如「指定池配额」出现池子配额表格）
- [ ] **参数保存**：修改策略参数 → 保存配置 → 重新加载 → 参数正确恢复
- [ ] **默认参数**：切换策略时，新策略的参数自动填充默认值

---

## P13 — 新增GDR：抽数转化效率

- [ ] **指标可选**：统计分析面板 GDR 下拉框中有 `抽数转化效率` 选项
- [ ] **计算正确**：消耗 16000 资源，在抽卡池抽了 100 次，单抽成本 160 → 效率 = 1.0
- [ ] **多池场景**：只有指定为抽卡池的池子计入效率计算

---

## 本轮新增 — 统计分析面板单位转换与参考线修复

### 条件变更检测

- [ ] **勾选「以抽数为单位」后重新运行**：运行一次分析后，勾选复选框，再次点击「运行分析」，应正常重新计算并覆盖结果（不应提示「所有选中项已有结果且条件未变」）
- [ ] **修改单抽消耗后重新运行**：修改单抽消耗数值框后，应能重新运行分析
- [ ] **取消勾选后恢复原始单位**：取消勾选→重新运行→资源剩余图表恢复原始资源数值

### 每池分布山脊线图（截止每池的GDR分布）

- [ ] **「资源剩余」山脊线图出现绿色虚线**：在「截止每池的GDR分布」中勾选「资源剩余」运行分析，每个池子子图上出现绿色虚线标记不抽卡基线
- [ ] **勾选「以抽数为单位」时绿色虚线位置联动**：基线值自动换算为抽数单位
- [ ] **轴标签显示单位**：使用抽数单位时，x轴标签显示「资源剩余 (抽)」

### 脆弱性分析山脊线图

- [ ] **「总览」tab 出现绿色虚线**：运行脆弱性分析后，总览山脊线图的每个池子子图上出现绿色虚线标记不抽卡基线
- [ ] **基线值与统计分析面板一致**：同一批模拟结果下，两处的基线值相同

### 表格单位转换

- [ ] **VaR/CVaR 表**：「资源剩余」指标名显示「资源剩余 (抽)」（勾选抽数单位时），数值为抽数
- [ ] **GDR 统计表**：同上，「资源剩余」行数值为抽数
- [ ] **最差情形/最好情形表**：同上
- [ ] **条件分布表**：当目标指标为「资源剩余」时，数值为抽数

### 图表单位标识

- [ ] **GDR 分布图**：勾选抽数单位后，hist/CDF 图轴标签和标题显示「(抽)」后缀
- [ ] **条件分布图**：当目标指标为「资源剩余」时，轴标签和标题显示「(抽)」后缀
- [ ] **最差/最好情形条件分布图**：「资源剩余」子图轴标签显示「(抽)」后缀
- [ ] **取消勾选后不再显示后缀**：取消勾选→重新运行→所有「(抽)」后缀消失

---

## P0 — 移除版本号打印

- [ ] **启动 GUI**：`python -m gacha_simulator.main`，观察终端/控制台输出，确认不再出现 `[VERSION] x.y.z` 字样
- [ ] **执行批量模拟**：在「批量模拟」Tab 点击开始，模拟完成后检查终端，确认全程无版本号打印

---

## P1 — 悬停/选中颜色区分

- [ ] **悬停效果**：鼠标悬停在任意表格的数据行上（不点击），悬停行背景应为淡蓝色（`#d6e8f5`），比选中色更浅
- [ ] **选中效果**：点击选中某行，该行背景应为深蓝色（`#308cc6`），文字变白色，且明显深于悬停色
- [ ] **配置面板所有表格均生效**：分别检查各表格的悬停/选中颜色（不要只测一个表）

> 注：嵌入控件（下拉框/数字框/复选框）的背景透明功能已移除。Qt QSS 对复杂控件的透明背景支持有缺陷，会破坏原生子控件渲染。当前嵌入控件保持原生样式，功能正常。

---

## P2 — 池配置表新增「分布编辑」列

### 池子模板表

- [ ] **表头确认**：模板表表头最后一列为「分布编辑」，列宽较窄（~80px），内容为浅灰文字「...双击编辑」
- [ ] **双击「分布编辑」列**：弹出分布编辑对话框（PoolDistributionDialog），可正常编辑并保存
- [ ] **双击其他列**（模板ID、类型、持续、单抽消耗、分布）：不弹出对话框，仅触发 Qt 默认的内联编辑行为
- [ ] **新增模板**：点击「添加」按钮后，新行最后一列自动出现「...双击编辑」
- [ ] **删除模板后**：表结构正常

### 池子配置表

- [ ] **表头确认**：池子表表头最后一列为「分布编辑」，列宽较窄（~80px），内容为「...双击编辑」
- [ ] **双击「分布编辑」列**：弹出分布编辑对话框，可正常编辑并保存
- [ ] **双击其他列**（启用、ID、名称、类型、开始、持续、单抽消耗、备注）：不弹出对话框
- [ ] **添加池子**：点击「添加」按钮，新行最后一列自动出现「...双击编辑」
- [ ] **复制池子**：选中池子后点击「复制选中」，新行最后一列出现「...双击编辑」（不会被复制出错误的旧内容）
- [ ] **从模板批量添加池子**：生成的池子行最后一列均有「...双击编辑」
- [ ] **加载已有配置文件**：加载后池子表最后一列正常显示「...双击编辑」
- [ ] **删除池子后**：表结构正常

---

## P6 — 画图资源可选换算抽数

- [ ] **UI 控件**：统计分析面板左侧「全局参数」下方出现「图表单位」分组，内含：
  - 复选框「☑ 以抽数为单位」
  - 数值框「单抽消耗」（默认值从池配置自动解析，如 160）
- [ ] **默认值自动解析**：加载配置后，单抽消耗数值框自动填入第一个池的消耗值
- [ ] **勾选后运行分析**：勾选「以抽数为单位」→ 运行分析 → 资源类图表（如「资源剩余」分布图、CDF图）的横轴数值变为抽数（原始值 ÷ 单抽消耗）
- [ ] **不勾选**：资源类图表显示原始资源数值，不受影响
- [ ] **手动修改单抽消耗**：修改数值框后运行分析，图表使用新的除数值

---

## P7 — 不抽卡资源水平参考线

- [ ] **NoDrawStrategy 已注册**：在 `core/strategy.py` 的 `STRATEGY_REGISTRY` 中存在 `no_draw` 条目，且 `internal=True`（用户不可见）
- [ ] **基线计算**：批量模拟完成后，不抽卡基线自动计算（一次额外模拟，静默执行）
- [ ] **GDR 分布图参考线**：勾选「资源剩余」的分布/CDF图表后运行分析，图表中出现绿色虚线标记「不抽卡基线」
- [ ] **条件分布图参考线**：当目标指标为「资源剩余」时运行条件分布分析，直方图上出现不抽卡基线绿色虚线
- [ ] **基线值合理**：不抽卡基线资源值 = 初始资源 + 随时间增长的总资源（无任何抽卡消耗），应明显高于模拟结果中的资源水平
- [ ] **抽数单位联动**：当同时勾选「以抽数为单位」时，不抽卡基线也自动换算为抽数
- [ ] **模拟不中断**：不抽卡基线计算失败时不影响主模拟流程（`try/except` 静默处理）

---

## ⚠ 当前无法验收（依赖未来调试/测试界面或需代码审查）

> 以下条目因缺少运行时数据展示手段或无法在 UI 中主动触发异常路径，无法通过现有 UI 进行人工验证。
> 待「调试模式」（`docs/调试模式实施计划.md`）或逐抽详情面板实现后再验收。

- [ ] **P10 — first_time_bonus 触发验证**：无 UI 展示单次抽卡触发了哪些额外资源（ft/首次获得奖励），当前只能通过最终资源总量间接推断
- [ ] **P10 — nth_time_bonus 触发验证**：无 UI 展示第 N 次获得时触发的额外资源，无法确认 `initial_count + 新抽到数` 的合计逻辑是否正确
- [ ] **P10 — excess_bonus 触发验证**：无 UI 展示超出阈值后的额外资源触发，无法确认阈值判断和每次获得都触发的逻辑
- [ ] **P10 — 额外资源叠加验证**：单次抽卡同时触发 resources_gained + first_time_bonus + nth_time_bonus + excess_bonus 时，无法确认各项是否正确叠加
- [ ] **P9 — card_counts 与 initial_count 分离**：无法在分析结果中直接查看 card_counts 的数值，只能通过 GDR（如 target_achievement）间接推断，无法排除 GDR 本身有 bug 导致两个错误互相抵消的可能
- [ ] **P10 — 兑换池 excess_bonus**：兑换池的 excess_bonus（如星辉兑换）触发逻辑无法在现有 UI 中验证
- [ ] **P13 — 抽数转化效率 池类型过滤**：池子类型（角色/武器 vs 兑换/资源）的推断和过滤逻辑正确性无法直观验证，只能通过构造极端场景（全兑换池→效率应为 0）间接推断
- [ ] **P7 — NoDrawStrategy 代码注册**：`STRATEGY_REGISTRY` 中 `no_draw` 条目是否存在且 `internal=True`，只能通过代码审查验证（UI 中策略下拉框不出现 `no_draw` 可间接确认 internal=True）
- [ ] **P7 — 基线计算失败不中断主流程**：`try/except` 静默处理异常是错误路径，UI 无法主动触发基线计算失败，只能通过代码审查验证

---

## 回归检查

- [ ] 批量模拟功能正常运行（P0 不应引入回归）
- [ ] 配置面板各表格基本交互正常（增删改、加载保存配置文件）
- [ ] 应用启动无 QSS 解析错误（控制台无警告）
- [ ] 策略注册表仍为 7 个用户可见策略（`no_draw` 标记为内部策略不显示）
- [ ] 不勾选「以抽数为单位」时，所有图表与 P6 实施前完全一致
