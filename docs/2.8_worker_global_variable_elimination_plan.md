# 2.8 Worker 全局变量完全消除方案

> 编写日期：2026-05-22
> 当前状态：部分完成（策略类不再直接依赖 `_wk_*`，但 worker 机制仍使用全局变量）

---

## 一、现状分析

### 1.1 当前全局变量清单

`service/batch_simulator.py` 中定义了 11 个模块级全局变量：

| 变量名 | 类型 | 默认值 | 用途 |
|--------|------|--------|------|
| `_wk_pools` | list | None | 所有池对象列表 |
| `_wk_schedule_mgr` | PoolScheduleManager | None | 池时间表管理器 |
| `_wk_end_time` | float | None | 模拟结束时间 |
| `_wk_pity_engine` | PityEngine | None | 保底机制引擎 |
| `_wk_resource_gain` | ResourceGain | None | 资源获取计算器 |
| `_wk_pity_state_init` | dict | None | 保底状态初始值 |
| `_wk_card_defs` | list | None | 卡牌定义列表 |
| `_wk_strategy_name` | str | 'smart' | 策略英文键名 |
| `_wk_strategy_params` | dict | {} | 策略参数 |
| `_wk_ssr_ids` | set | set() | SSR卡ID集合 |
| `_wk_stop_condition` | StopCondition | None | 停止条件 |

### 1.2 使用流程

```
主进程                                    子进程
──────                                    ──────
run_batch_parallel(env.*)
  │
  ├─ max_workers <= 1:
  │    _wk_init(env.*)  ──→  _wk_* = env.*
  │    for i in range(N):
  │      _wk_run_single((seed, target_specs, init_res))
  │        └─ 读取 _wk_* 全局变量
  │        └─ create_strategy(_wk_strategy_name, ...)
  │        └─ GachaService(_wk_pools, strategy, ...)
  │
  └─ max_workers > 1:
       MPPool(initializer=_wk_init, initargs=(env.*,))
       mp_pool.imap_unordered(_wk_run_single, tasks)
         └─ 每个子进程内 _wk_* 已被 initializer 设置
         └─ _wk_run_single 读取 _wk_* 全局变量
```

### 1.3 已完成的改进

- ✅ 策略类不再直接访问 `_wk_*` 全局变量（通过 `StrategyContext` 传入所需信息）
- ✅ `create_strategy()` 工厂函数不再需要 `_wk_pools`、`_wk_pity_engine` 等参数
- ✅ `SimulationEnv` 数据类已封装所有环境信息
- ✅ `SimulationEnvBuilder.from_config_store()` 已统一构建环境

### 1.4 仍存在的问题

1. **`_wk_init` + `_wk_*` 全局变量模式仍然存在**：虽然策略不再直接依赖，但 `_wk_run_single()` 仍通过全局变量获取环境信息
2. **单进程路径污染主进程全局状态**：`max_workers <= 1` 时在主进程调用 `_wk_init()`，全局变量可能被意外修改
3. **`run_batch_parallel` 参数过多**：13 个位置参数 + 5 个关键字参数，与 `SimulationEnv` 字段高度重叠
4. **`_wk_run_single` 的 args 只传递动态参数**：`args = (seed, target_specs, initial_resources)`，静态环境通过全局变量获取，两种参数传递方式不一致

---

## 二、消除方案

### 方案 A：SimulationEnv 作为唯一环境载体（推荐）

**核心思路**：将 `SimulationEnv` 作为 `_wk_run_single` 的唯一环境参数来源，消除所有 `_wk_*` 全局变量。

#### 步骤 2.8.1：重构 `_wk_run_single` 接受 `SimulationEnv`

```python
# 修改前
def _wk_run_single(args) -> Optional[Dict[str, Any]]:
    seed, target_specs, initial_resources = args
    # ... 使用 _wk_* 全局变量 ...

# 修改后
def _wk_run_single(args) -> Optional[Dict[str, Any]]:
    seed, target_specs, initial_resources, env = args
    # ... 使用 env.* 替代 _wk_* ...
```

但这里有一个关键问题：**`SimulationEnv` 需要可 pickle 序列化**才能在多进程间传递。

#### 步骤 2.8.2：验证 SimulationEnv 的可序列化性

`SimulationEnv` 的字段中，以下可能有序列化问题：
- `pity_engine`：自定义类，需验证是否可 pickle
- `schedule_mgr`：自定义类，需验证
- `resource_gain`：自定义类，需验证
- `stop_condition`：自定义类，需验证

**如果不可序列化**，有两种子方案：

**子方案 A1**：使用 `initializer` 传入 `SimulationEnv`（保留 initializer 模式，但用单一对象替代 11 个全局变量）

```python
_wk_env = None

def _wk_init(env):
    global _wk_env
    _wk_env = env

def _wk_run_single(args):
    seed, target_specs, initial_resources = args
    env = _wk_env
    # ... 使用 env.* ...
```

优点：避免序列化问题，减少全局变量从 11 个到 1 个
缺点：仍保留全局变量模式（但大幅简化）

**子方案 A2**：使 `SimulationEnv` 可序列化，通过 `args` 传入

```python
def _wk_run_single(args):
    seed, target_specs, initial_resources, env = args
    # ... 使用 env.* ...
```

优点：完全消除全局变量
缺点：每次任务都序列化/反序列化 `SimulationEnv`，有性能开销

#### 步骤 2.8.3：重构 `run_batch_parallel` 接受 `SimulationEnv`

```python
# 修改前
def run_batch_parallel(
    pools, schedule_mgr, end_time, pity_engine,
    resource_gain, pity_state_init, card_defs,
    target_specs, initial_resources, num_simulations,
    max_workers, seed=0, progress_callback=None,
    strategy_name='smart', strategy_params=None,
    on_result=None, ssr_ids=None, stop_condition=None,
) -> List[Optional[Dict[str, Any]]]:

# 修改后
def run_batch_parallel(
    env: SimulationEnv,
    target_specs: Dict[str, int],
    initial_resources: Dict[str, float],
    num_simulations: int,
    max_workers: int = 4,
    seed: int = 0,
    progress_callback: Optional[Callable] = None,
    strategy_name: str = 'smart',
    strategy_params: Optional[dict] = None,
    on_result: Optional[Callable] = None,
) -> List[Optional[Dict[str, Any]]]:
```

参数从 18 个减少到 11 个，其中 `env` 替代了 8 个独立参数（pools, schedule_mgr, end_time, pity_engine, resource_gain, pity_state_init, card_defs, ssr_ids, stop_condition）。

#### 步骤 2.8.4：更新所有调用入口

需要更新的文件（6 个）：

| 文件 | 当前调用方式 | 修改后 |
|------|-------------|--------|
| `gui/gacha_panel.py` | `run_batch_parallel(pools=env.pools, ...)` | `run_batch_parallel(env=env, ...)` |
| `gui/strategy_panel.py` | 同上 | 同上 |
| `gui/resource_search_panel.py` | 同上 | 同上 |
| `gui/strategy_comparison_panel.py` | 同上 | 同上 |
| `core/worst_impact.py` | 同上 | 同上 |
| `core/retreat_search.py` | 同上 | 同上 |

所有调用入口已经先构建 `SimulationEnv`，再展开为独立参数传入。重构后直接传入 `env` 即可。

#### 步骤 2.8.5：消除单进程路径的全局变量污染

```python
# 修改前
if max_workers <= 1:
    _wk_init(pools, schedule_mgr, ...)  # 污染主进程全局变量
    for i in range(num_simulations):
        result = _wk_run_single((s, target_specs, initial_resources))

# 修改后（子方案 A1）
if max_workers <= 1:
    for i in range(num_simulations):
        result = _run_single_with_env(env, (s, target_specs, initial_resources))

# 修改后（子方案 A2）
if max_workers <= 1:
    for i in range(num_simulations):
        result = _wk_run_single((s, target_specs, initial_resources, env))
```

---

### 方案 B：使用 `concurrent.futures.ProcessPoolExecutor` + 闭包

**核心思路**：用 `ProcessPoolExecutor` 替代 `multiprocessing.Pool`，通过 `initializer` 传入 `SimulationEnv`。

此方案与方案 A 的区别仅在多进程实现层，不影响接口设计。`ProcessPoolExecutor` 的 API 更现代，但 `initializer` 模式相同。

**不推荐**：与方案 A 本质相同，但需要额外适配 `imap_unordered` 的等价功能。

---

### 方案 C：使用 `multiprocessing.shared_memory` 或 `Manager`

**核心思路**：通过共享内存或 Manager 代理传递环境数据。

**不推荐**：
- 共享内存只支持基本类型，自定义对象需要手动序列化
- Manager 代理有性能开销和复杂度
- 对当前场景过度设计

---

## 三、推荐方案：A1（渐进式）

### 3.1 为什么推荐 A1

1. **最小改动量**：将 11 个全局变量合并为 1 个 `_wk_env`，改动集中在 `batch_simulator.py` 内部
2. **无序列化风险**：保留 `initializer` 模式，`SimulationEnv` 不需要跨进程序列化
3. **接口简化**：`run_batch_parallel` 参数从 18 个减少到 11 个
4. **向后兼容**：所有调用入口已使用 `SimulationEnv`，只需改为直接传入
5. **渐进路径**：A1 完成后，如果验证了 `SimulationEnv` 可序列化，可进一步升级到 A2 完全消除全局变量

### 3.2 实施步骤

| 步骤 | 内容 | 工作量 | 风险 |
|------|------|--------|------|
| 2.8.1 | 将 `_wk_*` 11 个全局变量合并为 `_wk_env: SimulationEnv = None` | 小 | 低 |
| 2.8.2 | 重构 `_wk_init()` 接受 `SimulationEnv` 单一参数 | 小 | 低 |
| 2.8.3 | 重构 `_wk_run_single()` 从 `_wk_env` 读取字段 | 小 | 低 |
| 2.8.4 | 重构 `run_batch_parallel()` 接受 `env: SimulationEnv` 参数 | 中 | 中 |
| 2.8.5 | 更新 6 个调用入口 | 中 | 低 |
| 2.8.6 | 单进程路径不再调用 `_wk_init()`，直接使用 `env` | 小 | 低 |
| 2.8.7 | 删除 `_wk_*` 全局变量和 `_wk_init()` 函数 | 小 | 低 |

### 3.3 预期代码变化

**batch_simulator.py 内部**：

```python
# 修改前：11 个全局变量 + _wk_init 11 个参数
_wk_pools = None
_wk_schedule_mgr = None
# ... 9 more ...

def _wk_init(pools, schedule_mgr, end_time, pity_engine,
             resource_gain, pity_state_init, card_defs,
             strategy_name, strategy_params, ssr_ids=None,
             stop_condition=None):
    global _wk_pools, _wk_schedule_mgr, ...
    _wk_pools = pools
    # ... 10 more ...

# 修改后：1 个全局变量 + _wk_init 1 个参数
_wk_env: Optional[SimulationEnv] = None

def _wk_init(env: SimulationEnv):
    global _wk_env
    _wk_env = env
```

**_wk_run_single 内部**：

```python
# 修改前
strategy = create_strategy(_wk_strategy_name, _wk_strategy_params)
service = GachaService(
    _wk_pools, strategy, stop_cond, target_set,
    schedule_manager=_wk_schedule_mgr,
    pity_engine=_wk_pity_engine,
    resource_gain=_wk_resource_gain,
    pity_state=pity_state,
    ssr_ids=_wk_ssr_ids,
)

# 修改后
env = _wk_env
strategy = create_strategy(env.strategy_name, env.strategy_params)
service = GachaService(
    env.pools, strategy, stop_cond, target_set,
    schedule_manager=env.schedule_mgr,
    pity_engine=env.pity_engine,
    resource_gain=env.resource_gain,
    pity_state=pity_state,
    ssr_ids=env.ssr_ids,
)
```

**run_batch_parallel 签名**：

```python
# 修改前：18 个参数
def run_batch_parallel(
    pools, schedule_mgr, end_time, pity_engine,
    resource_gain, pity_state_init, card_defs,
    target_specs, initial_resources, num_simulations,
    max_workers, seed=0, progress_callback=None,
    strategy_name='smart', strategy_params=None,
    on_result=None, ssr_ids=None, stop_condition=None,
):

# 修改后：11 个参数
def run_batch_parallel(
    env: SimulationEnv,
    target_specs: Dict[str, int],
    initial_resources: Dict[str, float],
    num_simulations: int,
    max_workers: int = 4,
    seed: int = 0,
    progress_callback=None,
    strategy_name: str = 'smart',
    strategy_params: Optional[dict] = None,
    on_result=None,
):
```

**调用入口（以 gacha_panel.py 为例）**：

```python
# 修改前
run_batch_parallel(
    pools=env.pools,
    schedule_mgr=env.schedule_mgr,
    end_time=env.end_time,
    pity_engine=env.pity_engine,
    resource_gain=env.resource_gain,
    pity_state_init=env.pity_state_init,
    card_defs=card_defs_list,
    target_specs=target_specs,
    initial_resources=env.initial_resources,
    num_simulations=N,
    max_workers=max_workers,
    seed=seed,
    progress_callback=...,
    strategy_name=strategy_key,
    strategy_params=config_store.strategy_params,
    on_result=collector.on_result,
    ssr_ids=env.ssr_ids,
)

# 修改后
run_batch_parallel(
    env=env,
    target_specs=target_specs,
    initial_resources=env.initial_resources,
    num_simulations=N,
    max_workers=max_workers,
    seed=seed,
    progress_callback=...,
    strategy_name=strategy_key,
    strategy_params=config_store.strategy_params,
    on_result=collector.on_result,
)
```

### 3.4 需要额外处理的问题

1. **`SimulationEnv` 缺少 `strategy_name` 和 `strategy_params` 字段**：
   当前 `SimulationEnv` 没有这两个字段，它们通过 `run_batch_parallel` 的独立参数传入。
   需要在 `SimulationEnv` 中添加：
   ```python
   @dataclass
   class SimulationEnv:
       # ... 现有字段 ...
       strategy_name: str = 'smart'
       strategy_params: Dict[str, Any] = dc_field(default_factory=dict)
   ```
   并在 `SimulationEnvBuilder.from_config_store()` 中从 `config_store.strategy_name` 和 `config_store.strategy_params` 填充。

2. **`_wk_stop_condition` 的处理**：
   当前 `stop_condition` 通过 `run_batch_parallel` 的独立参数传入，不在 `SimulationEnv` 中。
   需要在 `SimulationEnv` 中添加 `stop_condition` 字段，或在 `run_batch_parallel` 中保留此参数。
   推荐添加到 `SimulationEnv`：
   ```python
   stop_condition: Optional[Any] = None
   ```

3. **`_wk_card_defs` 的格式差异**：
   `SimulationEnv.card_defs` 是 `list[dict]`，而 `_wk_card_defs` 也是 `list[dict]`，格式一致，无需转换。

### 3.5 后续升级路径（A1 → A2）

完成 A1 后，如果验证了 `SimulationEnv` 的所有字段均可 pickle 序列化，可以进一步：

1. 将 `_wk_env` 全局变量也消除
2. 改为通过 `args` 传递 `SimulationEnv`
3. 完全消除全局变量模式

验证方法：
```python
import pickle
from gacha_simulator.service.batch_simulator import SimulationEnvBuilder, SimulationEnv
env = SimulationEnvBuilder.from_config_store(config_store)
pickled = pickle.dumps(env)
restored = pickle.loads(pickled)
```

如果序列化失败，需要为相关类（`PityEngine`、`PoolScheduleManager`、`ResourceGain` 等）实现 `__getstate__`/`__setstate__` 或使用 `dataclasses` 的默认序列化。

---

## 四、风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| `SimulationEnv` 字段不可序列化 | 低（A1 不需要） | 无 | A1 保留 initializer 模式 |
| 调用入口遗漏更新 | 低 | 中 | grep `run_batch_parallel` 确认所有入口 |
| 单进程路径行为变化 | 低 | 高 | 保留相同逻辑，仅改变参数来源 |
| `strategy_name`/`strategy_params` 迁移到 `SimulationEnv` 后调用入口需同步 | 中 | 低 | 逐步迁移，先保留 `run_batch_parallel` 参数 |

---

## 五、总结

推荐采用 **方案 A1**（渐进式），分 7 个步骤实施：

1. 将 11 个 `_wk_*` 全局变量合并为 1 个 `_wk_env`
2. 简化 `_wk_init()` 为单一参数
3. 更新 `_wk_run_single()` 从 `_wk_env` 读取
4. 重构 `run_batch_parallel()` 接受 `SimulationEnv`
5. 更新 6 个调用入口
6. 单进程路径直接使用 `env`
7. 删除旧全局变量

总工作量：**中**（约 2-3 小时），风险**低**。
