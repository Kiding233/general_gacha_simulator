# 最多目标卡面板 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] 前进法的 monotonicity 违反检测——当 `prob < last_valid_prob` 时是否记录警告或通知用户
- [ ] 后退法在全集成功率已超阈值时的提前返回路径——是否正确处理（无向后步骤记录的边界情况）
- [ ] 搜索迭代中的 `_should_stop` 标志——并行模拟是否在 stop 后仍会运行完整的 N 次再返回
- [ ] `compute_success_probability()` 的 `seed=0` 硬编码——不同迭代使用相同种子是否影响独立性
- [ ] 前进法在全部候选卡都可行时的边界情况——`last_valid_set` 是否为全集
- [ ] 后退法在全集不可行且无单张卡可达阈值的边界情况——`last_valid_set` 是否为 None（应在 UI 中提示用户）
- [ ] 权重表（desire/miss_cost/card_value）的默认值在所有候选卡上的一致性

---

*待首次审计完成后更新此文件。*
