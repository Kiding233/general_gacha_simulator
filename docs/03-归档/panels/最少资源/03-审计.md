# 最少资源面板 -- 审计

> 创建日期：2026-06-02
> 状态：**首次建档，未审计**

---

本模块尚未进行正式的代码审计。以下条目待后续补充：

- [ ] `_extract_cost_per_draw()` 多池不同成本场景下的 epsilon 精度——只返回第一个匹配值是否导致精度偏差
- [ ] 翻倍搜索上界在 `initial_resource_hint` 已经满足阈值时的早期返回路径——是否正确处理（翻倍次数为 0，直接进入二分阶段）
- [ ] 二分搜索在有噪声判定下的收敛性——噪声导致 `prob >= threshold` 误判是否产生震荡（`max_iterations` 是否足够）
- [ ] `_initial_resources_backup` 的深拷贝完整性——`_simulate_with_resource()` 每次覆盖 `draw_resource` 是否残留前次迭代的影响
- [ ] `max_iterations` 边界情况——当二分迭代被 `max_iterations` 截断时，`min_resource` 是否为有效近似
- [ ] 进度信号（`progress.emit`）百分比的计算基线——从 0-100% 的映射是否在所有分支路径上一致
- [ ] `ResourceSearchWorker.stop()` 设置 `_should_stop` 标志后，`_simulate_with_resource()` 内部仍在运行的 `run_batch_parallel()` 是否会被中断还是运行至完成
- [ ] `seed=0` 硬编码——搜索中所有迭代使用相同种子，噪声的相关性是否影响二分路径的展开

---

*待首次审计完成后更新此文件。*
