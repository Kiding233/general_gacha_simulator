# 项目计划汇总

> 最后更新：2026-05-29（第23版：理论审查缺陷全覆盖——新增 7 个理论改进计划 P26-P32）

---

## 一、已完成的计划（30 个）

| # | 日期 | 计划 | 说明 |
|---|------|------|------|
| 1 | 05-07 | gacha-simulator-implementation-plan | 初始实现：核心模拟引擎、GUI、配置系统 |
| 2 | 05-13 | code-refactor | 消除 GUI 层重复模拟环境构建，统一 GDR 成功率计算 |
| 3 | 05-13 | retreat-search | 退路搜索：最小额外资源/最大目标卡/Pareto 前沿 |
| 4 | 05-13 | retreat-vulnerability-analysis | 脆弱性分析：条件资源分布、核密度回归 |
| 5 | 05-13 | strategy-comparison-infra | 策略注册表、4 种策略工厂、Worker 策略选择 |
| 6 | 05-14 | worst-impact | 最差影响分析：条件分布下尾分位数评估 |
| 7 | 05-14 | retreat-search-phase2 | 退路搜索二期（Pareto 可视化、权重配置、子标签合并）— 部分完成，4 项低优先级待办 |
| 8 | 05-16 | 遗留代码清理 + 权重统一 + 关于页面 | 删除 ConvertAction/旧版保底/archive/visualization 文件、权重 txt 导入导出统一、GDR_REGISTRY 补全 target_collection、关于页面完善、Pride Versioning 版本号系统 |
| 9 | 05-16 | GDR 与成功率判断统一管理 | 合并两套注册表为 UNIFIED_GDR_REGISTRY、创建 SuccessChecker 统一成功判断、修复 5 个数值不一致 bug、消除 6 处硬编码、5 个面板下拉列表统一 |
| 10 | 05-17 | 流式模拟架构重构 | SharedResultCollector 边模拟边提取边丢弃、内存与 N 无关；逐抽真实资源替代线性插值/均摊近似；修复 9 项 bug |
| 11 | 05-17 | 过程分析功能（核心） | infer_events 双路径轨迹推断、compute_aa/bb/ab/ba 四种交叉统计分析、5种事件模式+4种成败模式、ProcessAnalysisPanel UI |
| 12 | 05-20 | 策略代码重构（5阶段） | CompactResult + Collector 模式 + StrategyContext + 统一策略体系 + ConfigStore + 停止条件注册表 + 策略比较面板 + 保底概率缓存 |
| 13 | 05-23 | 流式分析内存优化（方案A） | DrawSequenceExtractor 热力图数据从逐值列表改为预分箱直方图，10 万次模拟 heatmap 内存从 ~1.92 GB 降至 ~128 KB |
| 14 | 05-24 | implementation_plan 全量重写 | 基于理论参考.svg 重新理解理论架构，重写为四层模型（假设→机制→策略→GDR→决策）+ 10 类 93 条条目清单 |
| 15 | 05-25 | 最差影响分析修复 | 修复 1（策略 Smart→DrawTarget）+ 修复 2（max_workers 并行化）+ 修复 4（draw_resource 消除硬编码）+ 10 项正确性测试。修复 3（MAX_POOLS 动态上限）已搁置 |
| 16 | 05-25 | Worker 全局变量消除 | 11 个全局变量 → 2 个（仅多进程 initializer 使用）、单进程路径完全不碰全局变量、`run_batch_parallel` 参数 18→10、6 个调用入口更新、`SimulationEnv` 字段完备、160 项测试通过 |
| 17 | 05-26 | P16 保底系统关键缺陷修复 | 查询/修改分离 + 概率归一化（缺陷 9, 10） |
| 18 | 05-26 | P17 最差影响保底匹配修复 | 虚拟池 fnmatch 回退匹配原始池 ID（缺陷 20） |
| 19 | 05-26 | P2 过程分析续 | skip/ignore 精准判定 + 资源池事件 + Laplace 平滑 + low_sample 标记（缺陷 15, 16, 17） |
| 20 | 05-26 | P15 阶段一 脆弱性理论改进 | r_grid 扩展 + 局部 logistic 回归 + 边界溢出修复 |
| 21 | 05-26 | P3 Bootstrap 稳定性分析引擎 | BootstrapEngine + BootstrapResult + BCa + GPD + Hill 估计器（核心完成，UI 待做） |
| 22 | 05-26 | 小型修复 ×6 | resource_per_card NaN、CVaR 分数权重、空分布 NaN、DropRateBetweenT1T2 修正等 |
| 23 | 05-26 | P12-Phase1.5 GUI 性能优化 | Extractor 移入 Worker + 单次遍历 + 轻量传输 + 进度条即时反馈；GUI 1000 样本 10-15s → 2-3s |
| 24 | 05-26 | GDR 扩展（额外） | +4 新指标（resource_consumed/resource_per_card/draw_conversion_efficiency/target_card_draws）+ lower_is_better 支持 |
| 25 | 05-27 | P20 GUI 特有开销优化 (P0+P1) | P0 去除 collector 冗余累积 + P1 进度信号批处理；预期热启动 ~2.9s → ~2.1s |
| 26 | 05-27 | P11 Plotly 图表迁移 S1-S4 | 三层架构基础设施 + 8 面板迁移 + 8 项验收问题修复（xref crash / 池名截断 / 脆弱区间 / 分箱对齐 / PNG 导出 / Pareto 连线） |
| 27 | 05-25 | P7 项目条目实施 | project.md 9 条目 + 转变分析统一 + 策略参数 + GDR 扩展——12/12 全部完成（2026-05-27 归档） |
| 28 | 05-25 | P11 Plotly 图表迁移完整计划 | 三层架构设计 + 8 面板迁移方案 + 8 项修复（2026-05-27 归档，S5.3 已提取为独立计划） |
| 29 | 05-28 | P23 统计方法外部库替代实施 | 添加 scipy>=1.10 正式依赖、Bootstrap 标准路径委托给 scipy、消除隐藏依赖、32 项测试覆盖、P19/P4 外部库使用指南 |
| 30 | 05-28 | P24 EVT 尾部拟合 | 在 EmpiricalDistribution 核心层集成 GPD 尾部外推，6 步实施（evt_tail.py 新建 + distribution.py 改造 + worst_impact.py 截断处理 + 30 测试 + about_dialog 更新），零 GUI 代码改动覆盖全部面板 |

---

## 二、进行中的计划（部分条目已实施）

---

## 三、未执行的独立计划（12 个）

### P0：优先级执行路线图（元计划）

- **文件**：`docs/优先级执行路线图.md`（2026-05-26 创建，2026-05-28 重构为行动清单）
- **性质**：行动清单——按「先讨论完善→再下令实施→被阻塞→小修小补」四区组织，告诉你下一步该干什么，非独立实施计划
- **内容**：快速导航（当前最优先 3 项行动）、需要讨论完善的计划（阻塞决策/待审批/范围待定）、可以实施的计划（已敲定按优先级）、被阻塞项（含解除条件）、小修小补、依赖关系图、进度仪表盘、已完成里程碑
- **更新**：随计划完成/新增同步更新

### P2：过程分析功能（续）

- **设计文档**：`docs/superpowers/specs/archive/2026-05-15-process-analysis-design.md`（v5，已归档）
- **实施计划**：`docs/superpowers/plans/archive/2026-05-15-process-analysis.md`（v9，已归档）
- **状态**：**已完成 + 已归档**（2026-05-26）——skip/ignore 精准判定 + 资源池事件 + Laplace 平滑 + low_sample 标记

**已核实完成的项**：

| 功能 | 核实结果 |
|------|---------|
| 兑换池事件（exchange/no_exchange） | ✅ 已实现——`process_trace.py` 两路径均处理兑换池 |
| 保底名区分（pity_name） | ✅ 已实现——`pity_name` 在 `pity_hit` 事件中两路径均记录 |
| 资源池事件 | ✅ 已实现——新增 `resource_draw`/`resource_ignore` 事件类型（缺陷 15） |
| skip/ignore 判定修正 | ✅ 已实现——使用池可掉落列表判断 skip/ignore，修正 0 抽池误判为 ignore（缺陷 16） |
| 条件概率小样本保护 | ✅ 已实现——Laplace 平滑（Jeffreys Beta(0.5,0.5)）+ `low_sample` 标记（缺陷 17） |

**已搁置**：

| 功能 | 说明 |
|------|------|
| success_distribution UI 展示 | AB 分析中 `success_distribution` 已计算但未展示 UI。用户已确认搁置 |

- **已知局限**：小样本低概率下点估计不可靠，已改为 Wilson 得分区间方案（删除 Laplace 平滑），详见 `docs/archive/小样本概率估计改进计划.md`（已归档）。

### P3：Bootstrap 稳定性分析

- **实施计划**：`docs/Bootstrap稳定性分析改进计划.md`（2026-05-26 合并，旧文件 `docs/superpowers/plans/archive/2026-05-18-bootstrap-stability-analysis.md` 已归档）
- **状态**：**核心完成，引擎修复 + UI 待做**（2026-05-26）——BootstrapEngine + BootstrapResult + BCa + GPD + Hill 估计器已实现，面板未接入
- **目标**：为所有统计估计添加 Bootstrap 置信区间
- **核心思路**：不重新跑模拟，对已有 N 条结果做有放回抽样，零额外模拟成本
- **依赖**：无（纯数组操作，独立于所有其他计划）
- **已知局限**（待修复，详见 `docs/Bootstrap稳定性分析改进计划.md` §四.4.4 + §六）：
  - Jackknife 上限 1000 截断——n > 1000 时只用前 1000 个 leave-one-out 估计，需改为随机抽样
  - 厚尾检测未自动采纳——`detect_heavy_tail()` 返回建议但 `bootstrap_mean()` 不自动调用
  - BCa 在小 n 时不稳定——n < 50 时 Jackknife 加速因子 a 方差极大
  - m-out-of-n 的 m 选择策略缺失 + Hill k 选择缺失 + 离散 BCa 失效（理论审查发现）

### P4：自适应模拟次数与方差缩减

- **实施计划**：`docs/自适应模拟与方差缩减计划.md`（v4，2026-05-28 提取 EVT 为独立计划）
- **状态**：未开始
- **目标**：自适应精度模式（Task 1）；对偶变量法（Task 2）暂缓；**Task 3（EVT 尾部拟合）已提取为 P24 独立计划**
- **依赖**：流式重构（已完成）的 `on_result` 回调；P23（scipy 正式依赖，已完成）；P3（Bootstrap 引擎，核心已完成）
- **关联**：P3（Bootstrap）应在 P4 之前——Bootstrap 先实现通用框架，自适应停止在此基础上扩展
- **v4 变更**（2026-05-28）：Task 3 提取为独立计划 `docs/EVT尾部拟合独立计划.md`，P4 仅保留 Task 1（自适应停止）+ Task 2（对偶变量，暂缓）
- **v3 重写要点**（2026-05-28）：
  - Task 2 方案修正：v2 种子互补法（`seed_b = MAX_UINT32 - seed_a`）理论上不成立，已重写为正确的单次模拟内 `(u, 1-u)` 配对方案；复杂度从「低」升级为「中-高」，建议暂缓至 Task 1 完成后重新评估
  - Task 3 改用 scipy：手写 GPD MLE（~80行）替换为 `scipy.stats.genpareto.fit()`；R² 自动阈值替换为参数稳定性检测法（Coles 2001）→ **已提取为独立计划**
  - Task 1 补充 RSE 停止偏误缓解：连续两次达标才触发收敛
- **⚠ 已解决的理论问题**（2026-05-26 审查发现，v3 已处理）：
  - ~~对偶变量种子互补方案不正确~~ → v3 重写为正确方案（暂缓实施）
  - ~~EVT R² 自动阈值选择方法非标准~~ → v3 替换为稳定性检测法（已随 Task 3 提取）
  - ~~RSE 停止偏误~~ → v3 新增连续达标校验 + 保守阈值建议

### P8：方案搜索面板合并

- **实施计划**：`docs/面板合并可行性计划.md`（v4，2026-05-28 重写：纳入 StrategyPanel）
- **状态**：设计阶段，未实施
- **目标**：合并「最多目标卡」+「最少资源」+「退路分析→方案搜索」三个面板为统一「方案搜索」，新增完整时间线 Pareto + 退路点前进法，净减少 ~830 行，消除三处权重表重复
- **方案 A（推荐）**：三合一——StrategyPanel + ResourceSearchPanel + RetreatSearchPanel；方案 B（保守）：二合一——仅 StrategyPanel + ResourceSearchPanel
- **范围决策**：脆弱性分析面板（`RetreatPanel`）不合并——它是诊断工具，保留为独立子Tab提供退路起点数据
- **依赖**：**P11（Plotly 图表迁移，已完成）**

### P9：matplotlib 图表绘制代码重构 — ❌ 已放弃

- **实施计划**：`docs/archive/图表绘制代码重构方案.md`（v2，2026-05-24，2026-05-26 归档）
- **状态**：**已放弃**（2026-05-25 决策）
- **放弃原因**：被 **P11（Plotly 图表迁移）** 替代。两者互斥，P11 提供 HiDPI 原生支持 + 交互式图表 + 矢量级清晰度，收益远超 P9 的 matplotlib 代码整理
- **后续处理**：P9 中部分思路（统一图表接口、ChartSpec 中间表示）已被 P11 吸收；P9 中的临时文件泄漏修复在 P11 实施过程中一并解决
- **⚠ 与 P11（Plotly图表迁移）互斥**——已选择 P11

### P12：模拟性能优化

- **实施计划**：`docs/模拟性能优化方案.md`（v2，2026-05-25 初版 → 2026-05-26 更新进度）
- **状态**：**部分实施**——Phase 1 + Phase 1附加 + Phase 1.5 已完成，Phase 2/3 未开始
- **目标**：1000 样本从 3-5s → 0.3-0.5s；1000 万样本从 8-14h → 4-20 分钟（20 核）
- **分阶段渐进式实施**：

| 阶段 | 方案 | 预期加速 | 风险 | 周期 | 状态 |
|------|------|---------|------|------|------|
| Phase 1 | 算法优化（预构建 `_wk_target_set`、消除 per-simulation 重复构建） | 10-15% | 极低 | 半天 | ✅ 已完成（05-25） |
| Phase 1附加 | 脆弱性分析向量化（statsmodels → numpy 闭式解，~335x 加速） | 脆弱性分析 40-70s → <1s | 极低 | 半天 | ✅ 已完成（05-25） |
| Phase 1.5 | GUI 性能瓶颈（Extractor 移入 Worker + 单次遍历 + 精简传回 + 按需加载 + 进度条） | GUI 1000 样本 10-15s → 2-3s | 中 | 1-3 天 | ✅ 已完成（05-26） |
| Phase 2 | Numba 数值化内核（ID 映射系统 + 结构化 numpy dtype + @njit 核心循环 + 双路径回退） | 10-25x | 中 | 1-2 周 | ⬜ 未开始 |
| Phase 3 | 并行架构优化（共享内存替代 pickle 传输 + 改进 chunksize + 流式聚合） | 30-40%（在 Phase 2 之上）| 中 | 3-5 天 | ⬜ 未开始 |

- **Phase 1 实施细节**（2026-05-25）：
  - `batch_simulator.py`：新增 `_wk_target_set` 全局变量，在 `_wk_init()` 中一次构建 `TargetCardSet`，消除每次模拟的重复解析
  - `_wk_run_single` args 从 `(seed, target_specs, initial_resources)` 简化为 `(seed, initial_resources)`
  - 附带修复 `cli.py` 两个预存 bug（`pity_engine` 未赋值、`SmartStrategy` 旧接口）
  - 验证：pytest 160 passed，CLI seed=42 统计结果完全一致
- **Phase 1附加 实施细节**（2026-05-25）：
  - `vulnerability.py`：`_fit_local_polynomial` 完全重写为纯 numpy 闭式解（`_ll_fit()`），`_kernel_smooth_variance` 向量化
  - `pyproject.toml`：移除 `statsmodels` 依赖（项目唯一使用点）
  - 带宽选择从 statsmodels 默认 CV（~14s/pool）改为 Silverman 自适应带宽（~42ms/pool）
  - 验证：pytest 160 passed，闭式解与 statsmodels 差异 < 5e-12（同带宽下）
- **依赖**：Phase 1.5 零风险独立可执行；Phase 2 需 `numba` 可选依赖（`pip install numba`）；Phase 3 依赖 Phase 2
- **关键约束**：GUI、策略系统、停止条件的外部接口保持不变；Numba 不可用或策略不兼容时自动回退 Python 路径
- **不推荐方案**：GPU 加速（大概率负优化，数据传输开销 > 计算收益）、C/C++ 扩展（需用户手动搭编译工具链）
- **关联**：P10（Worker 全局变量消除，已完成）的 `_run_single()` 纯函数化为 Phase 1.5 的 Worker 内提取奠定了基础
- **⚠ 计划变更**（相对于初版）：
  - 新增 Phase 1附加——GUI vs CLI 性能差异调查中发现 statsmodels CV 带宽选择是脆弱性分析的隐藏瓶颈（每池 14s），非模拟嵌套问题
  - 新增 Phase 1.5——调查确认 GUI 10x 慢于 CLI 的根因是 `DrawSequenceExtractor` 在主进程串行处理（非模拟本身），原 Phase 1 的 20-30% 加速预估未覆盖此瓶颈

### P13：调试模式（单步抽卡）

- **实施计划**：`docs/调试模式实施计划.md`（v2，2026-05-28 更新）
- **状态**：设计中——已完成鸣潮式UI调研、PyQt6可实现性分析、浅色主题适配、视觉Mockup
- **目标**：新增「调试模式」Tab页，提供模拟抽卡游戏界面（鸣潮式左侧卡池竖列 + 右侧满铺展示区浮层覆盖）+ 下半区全量调试信息（等待控制、池快照2×2、操作日志、17项GDR全表、引擎状态、7种核心对象原始数据折叠卡片），QScrollArea单页滚动
- **核心操作**：抽取一次 / 抽取十次（UI层循环，不动引擎）/ 等待操作5种模式（自定义/+1h/+1d/跳到下池开放后/跳到池关闭前）/ 历史记录QDialog弹窗
- **四阶段**：Phase 1 核心单步逻辑（GachaService扩展 + GDR实时计算 + 原始数据采集）→ Phase 2 上半区游戏界面（侧栏+展示区+十连+等待）→ Phase 3 下半区调试信息（选项栏+日志+GDR表+原始数据）→ Phase 4 MainWindow集成
- **依赖**：无（独立可执行）
- **显式搁置**：事件展示——待 `docs/implementation_plan.md` 中事件定义问题解决后再讨论
- **扩展可能**：回退步数、断点条件、双策略对比模式、导出调试日志

### P15：脆弱性分析理论严谨性审查与改进

- **审查与改进方案**：`docs/脆弱性分析理论严谨性审查与改进方案.md`（v1，2026-05-25）
- **状态**：**阶段一已完成**（2026-05-26）——r_grid 扩展 + 局部 logistic 回归 + 边界溢出修复；**问题 H（直方图分箱边界）已修复**（2026-05-27）——新增 `_detect_step_size()` + `_compute_aligned_bins()`，按 cost_per_draw 格点分箱 + R₀ 对齐，符合 GDR 分箱报告建议；阶段二、三待实施
- **目标**：修复脆弱性分析的理论缺陷，提升统计严谨性
- **发现的 9 个问题**（按严重度）：

| 严重度 | 问题 | 核心发现 |
|--------|------|---------|
| 🔴 严重 | A: r_grid 边界硬限制 | 脆弱区间无法涵盖数据范围外的 100% 失败率区域 |
| 🔴 严重 | B: LLR 左边界偏差 | 低资源区 P(失败\|r) 被系统性低估 |
| 🟡 中等 | C: 二元响应用线性回归 | 预测值需裁剪、异方差被忽略；局部逻辑回归更合适（Signorini & Jones 2004） |
| 🟡 中等 | D: 带宽选择非回归最优 | Silverman 规则为密度估计设计，非回归最优（Fan & Gijbels 1995, 1996） |
| 🟡 中等 | E: 无多重性校正 | 200 格点同时阈值判断产生多重检验问题（Härdle & Mammen 1991） |
| 🟡 中等 | F: 无 persistence 过滤 | 单格点宽的噪声尖峰可能被误判为脆弱区间 |
| 🟢 低等 | G: 全局等带宽 | 不适应局部数据密度变化（Fan & Gijbels 1995） |
| 🟢 低等 | H: 直方图分箱边界 | 重尾资源分布下低资源区分辨率可能不足 |
| 🟢 低等 | I: 因果性假设 | UI 缺少关联 vs 因果的声明 |

- **改进分三阶段**：
  - 阶段一（3-5 天）：扩展 r_grid、LLR 边界偏差缓解、局部逻辑回归替代 LLR
  - 阶段二（2-4 天）：RSC 带宽选择、Wild Bootstrap 同时置信带、persistence 过滤
  - 阶段三（1-2 天）：KNN 自适应带宽、因果性 UI 注记
- **文献支撑**：12 篇核心文献
- **依赖**：无（独立可执行）
- **向后兼容**：所有改进通过新增参数控制默认值，当前行为不变

### P16：保底系统关键缺陷修复（NEW — 理论审查驱动）

- **来源**：`docs/reports/全模块理论严谨性系统性审查.md` 缺陷 9、10
- **状态**：**已完成**（2026-05-26）
- **目标**：修复 2 项保底系统的严重缺陷
  1. **缺陷 9**：`get_pity_probabilities()` 调用 `before_draw()` 递增保底计数器——PityReserveStrategy 每次查询概率都会触发「幽灵抽卡」，保底状态失真 → **已修复**：查询/修改分离
  2. **缺陷 10**：保底目标 ID 不在池奖励中时概率未归一化——硬保底场景概率和远小于 1.0 → **已修复**：概率归一化
- **依赖**：无（独立可执行）
- **风险**：缺陷 9 的修复涉及 `PityEngine.before_draw` 的查询/修改分离，需确保不影响正常抽卡流程

### P17：最差影响保底匹配修复（NEW — 理论审查驱动）

- **来源**：`docs/reports/全模块理论严谨性系统性审查.md` 缺陷 20
- **状态**：**已完成**（2026-05-26）——虚拟池 fnmatch 回退匹配原始池 ID
- **目标**：修复最差影响分析中保底引擎通配符匹配静默失败——虚拟池 ID `_worst_impact_pool_0` 不匹配原始保底的特定池模式（如 `pools=event_banner`），保底被静默丢弃，模拟在无保底状态下运行
- **依赖**：无（独立可执行，仅涉及 `worst_impact.py` 的 `_build_pity_engine`）
- **关联**：修复后可重新评估修复 3（MAX_POOLS 动态上限，当前已搁置）的必要性——有保底后连续成功次数可能显著增加

### P18：小样本概率估计改进 + Bootstrap 引擎修复与 UI 集成（NEW — P12-Phase1.5 后发现的短板）

> **注**：原 P18「拉普拉斯平滑与 Bootstrap 改进」已拆分为两个独立计划，旧文件 `docs/archive/拉普拉斯平滑与Bootstrap改进计划.md` 已归档。

- **实施计划**：
  - ~~`docs/小样本概率估计改进计划.md`~~ → **已完成并归档**（2026-05-28，→ `docs/archive/`）
  - `docs/Bootstrap稳定性分析改进计划.md`（Bootstrap 引擎修复 + UI 集成，合并了旧 Bootstrap 计划）
- **状态**：**部分完成**——小样本概率估计改进已实现并归档；Bootstrap 修复 + UI 集成待做
- **目标**：三项改进——
  1. ~~**小样本概率估计改进**~~ ✅：删除 Laplace 平滑，改用 Wilson 得分区间 + N<5 计数回退
  2. **Bootstrap 已知问题修复**：Jackknife 随机抽样（n>1000）+ auto_heavy_tail 参数（自动切换到 GPD-param Bootstrap）+ BCa n<50 守卫 + m 选择策略 + Hill k 选择 + 离散 BCa 守卫
  3. **Bootstrap UI 集成**：分析面板 + 过程分析面板表格 CI 列（MVP），后续扩展其余面板 + 图表 CI
- **依赖**：无（两项独立可并行，建议按序推进）
- **关联**：P2（过程分析）、P3（Bootstrap 引擎）

### P19：比较分析面板重构与结果数据管理层设计（NEW — 研究报告驱动）

- **实施计划**：`docs/比较分析面板重构与结果数据管理层设计.md`（2026-05-26，第2版）
- **来源**：`docs/风险决策理论在抽卡机制建模中的应用报告.md` + `docs/玩家社区抽卡策略比较方法论.md` 研究报告启发
- **状态**：**设计中**（计划待审批）
- **目标**：三项架构改进——
  1. **ResultStore 结果数据管理层**：可容纳多个命名数据集的持久化存储（`StoredDataset` + `CachedAnalysis`），将模拟与分析分离
  2. **数据管理 Tab**：集中式数据集浏览、加载、重命名、删除、导入/导出，替代各面板独立切换
  3. **比较分析面板**（原策略比较面板重构）：从 ResultStore 选取 2+ 数据集，进行随机占优检验（FSD/SSD）、KS/MWU 分布差异检验、Cohen's d 效应量、帕累托前沿分析
- **关键设计决策**：
  - 集中式数据管理（一个数据集加载到所有面板），非各面板独立切换——内存友好（仅 ~2-5 MB 原始数据在内存，其余磁盘）
  - 分析结果缓存（`CachedAnalysis` <500 KB/数据集），首次加载计算后缓存，切换和比较直接使用
  - 比较基于独立种子 + 分布检验（KS/随机占优），不依赖同种子配对
  - 废弃空白 Tab「敏感度分析」，鲁棒性测试作为比较分析的后续可选模式
- **依赖**：GDRContext 序列化评估（`core/gdr.py` 小幅修改）
- **三阶段实施**：
  - 第一阶段：ResultStore + 数据管理 Tab + MainWindow 集成 + 面板验证
  - 第二阶段：ComparisonAnalyzer 引擎 + 比较分析面板 UI（CDF 叠加图 + 占优矩阵 + 指标对比表）
  - 第三阶段：高级可视化（帕累托前沿 + 效应量热力图 + 雷达图）+ 鲁棒性分析模式
- **影响**：`core/result_store.py`（新增）、`gui/data_manager_panel.py`（新增）、`gui/comparison_analysis_panel.py`（重写）、`gui/main_window.py`（修改）；各分析面板几乎不改

### P22：多资源类型分布（NEW — 从 P7-P8 和 P11-S5.3 提取）

- **实施计划**：`docs/多资源类型分布实施方案.md`（v1，2026-05-27）
- **来源**：从 `docs/项目条目实施计划与调查报告.md` P8 和 `docs/Plotly图表迁移实施方案.md` S5.3 提取并扩展
- **状态**：**设计中**（计划待审批）
- **目标**：消除资源类型硬编码——GDR 层、分析面板、其他 4 个面板、不抽卡参考线全部支持按资源类型切换
- **五阶段实施**：GDR 层参数化（TDD）→ 分析面板 UI + 图表 → 其他面板扩展 → 不抽卡参考线扩展 → 清理
- **依赖**：P11 Plotly 迁移 S1-S4（已完成）
- **被依赖**：无
- **涉及文件**：~9 文件，+160 行测试 + ~190 行修改

### P25：EVT 改进——离散型与退化分布处理（NEW — 2026-05-28）

- **实施计划**：`docs/EVT改进——离散型与退化分布处理.md`（v1，2026-05-28）
- **来源**：P24 实施后的理论讨论——DGPD 评估、几何分布与 GPD 兼容性、17 种 GDR 分布分类、退化数据自动跳过
- **状态**：**已部分实施**——自动退化检测（`_count_distinct`）已实现 + 7 项测试；计划文档待审批
- **目标**：修正 EVT 对退化/离散分布的错误适用——
  1. **DGPD 评估**：不引入（连续 GPD 近似不足时，经验分位数已足够精确）
  2. **几何分布分析**：几何分布属于 Gumbel MDA（ξ=0），GPD 理论完全适用；问题在于离散格点而非分布族
  3. **17 种 GDR 分类**：A 类 9 种完全适用、B 类 6 种边际适用（取决于不同取值数）、C 类 2 种完全不适用（all_targets 二元 + weapon_character_ratio 退化）
  4. **自动退化检测**：`EmpiricalDistribution._count_distinct()` 惰性计算不同取值数，< 20 → 跳过 EVT（已实现）
- **已实现**：`distribution.py`（+20 行：`_count_distinct` 方法 + `_evt_quantile`/`_evt_cvar` 退化检测）+ `tests/core/test_distribution.py`（+7 项测试）
- **待实施**：更新 `about_dialog.py` 算法说明（注明退化分布自动跳过 EVT）
- **依赖**：P24（已完成）

---

### P26：退路搜索与资源目标搜索理论改进（NEW — 理论审查驱动）

- **实施计划**：`docs/退路搜索与资源目标搜索理论改进计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/全模块理论严谨性系统性审查.md` 缺陷 24/25/26 + `docs/reports/补充模块理论严谨性审查.md` 缺陷 R1-R6
- **状态**：**设计中**（计划待审批）
- **目标**：修复三个搜索算法（最少资源/最多目标卡/Pareto 前沿）的 10 项理论缺陷
  1. **缺陷 R1/26**：二分搜索 + 蒙特卡洛噪声 → SPRT 序贯采样替代固定 N
  2. **缺陷 24/R2**：Pareto 声称降级（UI「贪心近似」）+ 支配性过滤
  3. **缺陷 25/R3**：翻倍上界统一 + 上界不足检测
  4. **缺陷 R4**：随机克里金/贝叶斯优化共享模拟信息（长期）
  5. **缺陷 R5**：错失代价权重基数解释 + 替代排序方案
  6. **缺陷 R6**：epsilon 关联蒙特卡洛噪声水平
- **三阶段实施**：阶段一（低风险 UI 修复 2-3 天）→ 阶段二（SPRT 二分搜索 3-5 天）→ 阶段三（可选深化）
- **依赖**：P3/P18（Bootstrap CI 方法复用）；**建议先于 P8（面板合并）执行**——避免在合并代码中固化有缺陷的算法
- **关联**：P8（面板合并——本计划修复后的算法为合并目标）

### P27：转变分析统计严谨性改进（NEW — 理论审查驱动）

- **实施计划**：`docs/转变分析统计严谨性改进计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/补充模块理论严谨性审查.md` 缺陷 T1-T5
- **状态**：**设计中**（计划待审批）
- **目标**：为 2×2 马尔可夫转移矩阵补充不确定性量化 + 假设诊断
  1. **缺陷 T1**：转移概率从点估计 → Wilson CI + Bootstrap 同时置信域
  2. **缺陷 T3**：马尔可夫假设诊断（资源消耗路径依赖量化）
  3. **缺陷 T2**：零分母返回 NaN + low_sample 标记
  4. **缺陷 T4**：多池转移 FDR 多重比较校正
  5. **缺陷 T5**：事件-成败与池-池转变 UI 信息冗余说明
- **补充改进**（补充审查 §2.4）：TransitionMatrix 附加连续度量（mean_resource_consumed_given_success/failure）
- **三阶段实施**：阶段一（CI + 零分母修复 + 连续度量 2-3 天）→ 阶段二（假设诊断 + FDR 2-3 天）→ 阶段三（Bootstrap 同时置信域，依赖 P3/P18）
- **依赖**：P3（Bootstrap 引擎）、P18（Bootstrap 修复）

### P28：每池GDR分析统计严谨性改进（NEW — 理论审查驱动）

- **实施计划**：`docs/每池GDR分析统计严谨性改进计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/补充模块理论严谨性审查.md` 缺陷 P1-P7 + `docs/reports/全模块理论严谨性系统性审查.md` 缺陷 17（部分）
- **状态**：**设计中**（计划待审批）
- **目标**：修复每池 GDR 分析的 7 项理论缺陷
  1. **缺陷 P1**：K 个池的多重比较 FDR 校正（FWER 34%+ at K=8）
  2. **缺陷 P2**：Simpson 悖论诊断——CV 异质性检测 + UI 指示器
  3. **缺陷 P3**：Laplace 平滑 → 经验贝叶斯收缩（Efron & Morris, 1975）
  4. **缺陷 P4**：BA 似然比分母为零 → NaN + low_sample（替代 inf）
  5. **缺陷 P5**：单池 GDR 的 pseudo_compact 补全 + GDR 依赖声明
  6. **缺陷 P6/P7**：已知回退路径 + 特殊池类型文档化
- **三阶段实施**：阶段一（Simpson 诊断 + inf 修复 1-2 天）→ 阶段二（FDR + 经验贝叶斯 + pseudo_compact 3-5 天）→ 阶段三（可选深化）
- **依赖**：P2（过程分析续，已完成）、P3/P18（Bootstrap）、P22（多资源类型分布）

### P29：GDR系统理论文档补充（NEW — 理论审查驱动）

- **实施计划**：`docs/GDR系统理论文档补充计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/全模块理论严谨性系统性审查.md` 缺陷 4/5
- **状态**：**设计中**（计划待审批）
- **目标**：为 GDR 系统的两个效用类指标补充公理化文档 + 修正默认行为
  1. **缺陷 4**：`weighted_satisfaction` 公理化文档（可加性假设/无损失厌恶/未归一化）+ 归一化选项
  2. **缺陷 5**：`total_card_value` 非目标卡默认权重 1.0 → 0.0（破坏性变更）+ 稀有度自动推断（长期）
- **涉及文件**：`gdr.py`（docstring + 默认值修正）、`analysis_panel.py`（归一化 UI 复选框）
- **依赖**：P22（多资源类型分布——互补但不重叠）
- **注意**：默认值变更（1.0→0.0）为破坏性变更，需在发布说明中标注

### P30：策略系统与保底理论文档补充（NEW — 理论审查驱动）

- **实施计划**：`docs/策略系统与保底理论文档补充计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/全模块理论严谨性系统性审查.md` 缺陷 11/12/13/14
- **状态**：**设计中**（计划待审批）
- **目标**：文档化策略和保底系统的设计决策——4 项缺陷均不涉及代码错误，核心工作是理论澄清
  1. **缺陷 12**：策略启发式性质文档化 + UI「启发式结果」标注（非贝尔曼最优，但选择合理——Simon 满意原则）
  2. **缺陷 13**：策略比较统一效用框架——多维雷达图 + 元目标选择器（成功率/资源效率/稳健性）
  3. **缺陷 14**：StrategyContext 信息完备性文档化 + `future_resource_gains` 增量补充
  4. **缺陷 11**：保底多管道优先级文档化 + `warnings.warn()` 冲突检测
- **涉及文件**：`strategy.py`（docstring）、`pity.py`（docstring + 冲突检测）、策略比较面板（UI 标注）
- **依赖**：P16（保底缺陷修复，已完成）、P19（比较分析面板重构——多维比较受益）

### P31：最差影响分析剩余缺陷改进（NEW — 理论审查驱动）

- **实施计划**：`docs/最差影响分析剩余缺陷改进计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/全模块理论严谨性系统性审查.md` 缺陷 21/22/23/27/28
- **状态**：**设计中**（计划待审批）
- **前置**：P17（最差影响保底匹配修复，已完成）
- **目标**：修复 P17 未覆盖的 5 项剩余缺陷
  1. **缺陷 27**：条件分布分位数 → 新模拟的逻辑跳跃——UI 说明 + 长期接续模拟方案
  2. **缺陷 22/28**：虚拟池同质化假设——敏感度分析滑块 + 可配置池参数序列（长期）
  3. **缺陷 21**：MAX_POOLS 动态上限 + 截断检测（重新评估 P14 修复 3 搁置决策）
  4. **缺陷 23**：无资源收入假设——UI 标注保守下界性质
- **注意**：P17 修复保底匹配后——有保底状态下连续成功池数增加——放大了缺陷 21 和 27 的影响，需重新评估
- **依赖**：P17（已完成）、P24/P25（EVT 尾部拟合）

### P32：分布尾部估计改进（NEW — 理论审查驱动）

- **实施计划**：`docs/分布尾部估计改进计划.md`（v1，2026-05-29）
- **来源**：`docs/reports/补充模块理论严谨性审查.md` 缺陷 B1/B2/B5/B6
- **状态**：**设计中**（计划待审批）
- **目标**：为最好/最差情形分析补充不确定性量化 + 理论文档
  1. **缺陷 B2**：经验分位数尾部不稳定性 → Bootstrap SE + GPD 尾部外推委托（复用 P24）
  2. **缺陷 B1**：BestCaseAnalysis 缺乏公理化基础 → UI「乐观情形分析」+ docstring 说明
  3. **缺陷 B5/B6**：full_report 同步阻塞文档 + 条件顶部 low_sample 标记
- **依赖**：P24（EVT 尾部拟合，已完成）、P25（EVT 退化检测，部分已实施）、P3（Bootstrap 引擎）

---

### ~~P24：EVT 尾部拟合~~ ✅ 已完成（2026-05-28，已归档）

### P21：GDR 分箱与绘图策略实施（NEW — 2026-05-27）

- **实施计划**：`docs/GDR分箱与绘图策略实施计划.md`（2026-05-27）
- **来源**：`docs/reports/GDR分箱与绘图策略报告.md`（17 种 GDR 的离散性分类、分箱与绘图策略研究）
- **状态**：**设计中**（计划待审批）
- **目标**：将报告中的分箱/绘图建议落实到 `analysis_panel.py` 的 `_is_discrete` + `_hist_params` 方法，修正 7 项已知问题
- **三阶段实施**：
  - 阶段一（<1 天）：A 类有限格点检测 + B₁ 步长检测（复用 vulnerability 的 `_detect_step_size`）+ inf 处理
  - 阶段二（<1 天）：C 类 KDE 叠加 + B₁ 分箱起点偏移对齐
  - 阶段三（<1h）：`all_targets` 二元 GDR 专用展示
- **已完成前置**：P15 问题H（脆弱性分箱策略对齐报告）已在 2026-05-27 实施，`vulnerability.py` 中的步长检测和分箱偏移函数可复用
- **涉及文件**：`analysis_panel.py` + `chart_spec.py`/`plotly_charts.py` + `core/distribution.py`（公共函数提取）

### ~~P20：GUI 特有开销优化方案~~ ✅ 已完成（P0+P1 已实施，2026-05-27）

- **实施计划**：`docs/archive/GUI性能优化方案.md`（2026-05-27）
- **状态**：**已完成并归档**——P0（去除 collector 冗余累积）+ P1（进度信号批处理）已实施；P2-P5 不实施
- **成果**：消除主进程 1000 次 `extract_aggregate` 串行调用（~0.3-0.5s），进度信号从 1000 次降至 ~50 次；Worker 不再传回 compact dict；预期热启动 ~2.9s → ~2.1s

### 小型修复（✅ 已完成，2026-05-26）

以下审查发现的缺陷已修复：

| # | 模块 | 缺陷 | 修复 |
|---|------|------|------|
| 3 | GDR | `resource_per_card` 返回 `inf` 破坏聚合 | `inf` → `nan`，聚合时排除 |
| 1 | 分布 | CVaR ceil 偏差（小样本） | 分数权重计入尾样本 |
| 2 | 分布 | 空 EmpiricalDistribution 静默返回 0 | 返回 `nan` |
| 7 | GDR | `DropRateBetweenT1T2` 忽略 `t` 参数 | 修正或废弃该函数 |
| 18 | 流式 | SharedResultCollector 缺线程安全声明 | 文档注释 |
| 19 | 流式 | 热图分箱尾部聚集 | 动态上限或警告 |

### 全模块理论严谨性系统性审查

- **审查文件**：`docs/reports/全模块理论严谨性系统性审查.md`（v1，2026-05-26）
- **审查范围**：10 个已实现模块 + 4 个未实施计划
- **总体发现**：28 项理论缺陷（4 严重 + 17 中等 + 7 低等）+ 5 项计划理论风险
- **缺陷→计划全覆盖映射**（2026-05-29 完成）：

| 缺陷 | 模块 | 严重度 | → 计划 | 状态 |
|------|------|--------|--------|------|
| 1 | 分布 CVaR ceil | 🟡 中等 | 小型修复 ×6 | ✅ 已修复 |
| 2 | 分布 空分布返回 0 | 🟢 低等 | 小型修复 ×6 | ✅ 已修复 |
| 3 | GDR resource_per_card inf | 🔴 严重 | 小型修复 ×6 | ✅ 已修复 |
| 4 | GDR weighted_satisfaction | 🟡 中等 | **P29** | 📋 设计中 |
| 5 | GDR total_card_value 1.0 | 🟡 中等 | **P29** | 📋 设计中 |
| 6 | GDR 资源单态假设 | 🟡 中等 | P22 | 📋 设计中 |
| 7 | GDR DropRateBetweenT1T2 | 🟢 低等 | 小型修复 ×6 | ✅ 已修复 |
| 8 | GDR weapon_character_map | 🟢 低等 | 已知问题 | 📋 已知 |
| 9 | 保底 幽灵抽卡副作用 | 🔴 严重 | P16 | ✅ 已修复 |
| 10 | 保底 概率未归一化 | 🔴 严重 | P16 | ✅ 已修复 |
| 11 | 保底 多管道无优先级 | 🟡 中等 | **P30** | 📋 设计中 |
| 12 | 策略 不满足贝尔曼最优 | 🔴 严重 | **P30**（文档化） | 📋 设计中 |
| 13 | 策略 无统一效用函数 | 🟡 中等 | **P30** | 📋 设计中 |
| 14 | 策略 信息不完备 | 🟢 低等 | **P30** | 📋 设计中 |
| 15 | 过程分析 资源池缺失 | 🟡 中等 | P2 | ✅ 已修复 |
| 16 | 过程分析 skip/ignore 判定 | 🟡 中等 | P2 | ✅ 已修复 |
| 17 | 过程分析 条件概率无保护 | 🟡 中等 | P2 + P28(P4) | ✅ P2 已修复 / P4 部分 |
| 18 | 流式 缺线程安全声明 | 🟢 低等 | 小型修复 ×6 | ✅ 已修复 |
| 19 | 流式 热图分箱聚集 | 🟢 低等 | 小型修复 ×6 | ✅ 已修复 |
| 20 | 最差影响 保底匹配失败 | 🔴 严重 | P17 | ✅ 已修复 |
| 21 | 最差影响 MAX_POOLS 截断 | 🟡 中等 | **P31** | 📋 设计中 |
| 22 | 最差影响 虚拟池同质化 | 🟡 中等 | **P31** | 📋 设计中 |
| 23 | 最差影响 无资源收入假设 | 🟢 低等 | **P31** | 📋 设计中 |
| 24 | 退路搜索 Pareto 不严格 | 🔴 严重 | **P26** | 📋 设计中 |
| 25 | 退路搜索 上界可能失败 | 🟡 中等 | **P26** | 📋 设计中 |
| 26 | 退路搜索 二分噪声振荡 | 🟡 中等 | **P26** | 📋 设计中 |
| 27 | 最差影响 逻辑跳跃 | 🟡 中等 | **P31** | 📋 设计中 |
| 28 | 最差影响 高成本池连续 | 🟡 中等 | **P31** | 📋 设计中 |
| A-I | 脆弱性分析 9 项 | 混合 | P15 | 📋 阶段一完成 |
| — | P3 m/k 选择策略 | 🟡 中等 | P18 | 📋 待做 |
| — | P4 对偶变量方案 | 🔴 严重 | P4 v3 | 📋 已修正 |
| — | P4 RSE 停止偏误 | 🟡 中等 | P4 v3 | 📋 已修正 |
| — | P4 EVT R² 非标准 | 🟡 中等 | P4 v3 → P24 | ✅ 已修正 |

### 补充模块理论严谨性审查

- **审查文件**：`docs/reports/补充模块理论严谨性审查.md`（v1，2026-05-26）
- **审查范围**：4 个补充模块（最好/最差情形、转变分析、最少资源与最多目标卡、每池GDR）
- **总体发现**：24 项理论缺陷（7 严重 + 8 中等 + 9 低等/已知）
- **缺陷→计划全覆盖映射**（2026-05-29 完成）：

| 缺陷 | 模块 | 严重度 | → 计划 | 状态 |
|------|------|--------|--------|------|
| B1 | BestCaseAnalysis 缺理论基础 | 🟡 中等 | **P32** | 📋 设计中 |
| B2 | 经验分位数尾部不稳定 | 🔴 严重 | **P32** | 📋 设计中 |
| B3 | CVaR ceil（已知） | 🟡 中等 | 小型修复 ×6 | ✅ 已修复 |
| B4 | 空分布（已知） | 🟢 低等 | 小型修复 ×6 | ✅ 已修复 |
| B5 | full_report 同步阻塞 | 🟢 低等 | **P32** | 📋 设计中 |
| B6 | 条件顶部无样本量下限 | 🟢 低等 | **P32** | 📋 设计中 |
| T1 | 转移概率无 CI/SE | 🔴 严重 | **P27** | 📋 设计中 |
| T2 | 零分母静默归零 | 🟡 中等 | **P27** | 📋 设计中 |
| T3 | 马尔可夫假设可能不成立 | 🔴 严重 | **P27** | 📋 设计中 |
| T4 | 多池转移多重比较 | 🟡 中等 | **P27** | 📋 设计中 |
| T5 | 事件-成败 UI 冗余 | 🟢 低等 | **P27** | 📋 设计中 |
| R1 | 二分搜索噪声无收敛 | 🔴 严重 | **P26** | 📋 设计中 |
| R2 | Pareto 声称误导 | 🔴 严重 | **P26** | 📋 设计中 |
| R3 | 翻倍上界不一致 | 🟡 中等 | **P26** | 📋 设计中 |
| R4 | 模拟不共享信息 | 🟡 中等 | **P26**（长期） | 📋 设计中 |
| R5 | 错失代价无基数解释 | 🟡 中等 | **P26** | 📋 设计中 |
| R6 | epsilon 缺乏统计依据 | 🟢 低等 | **P26** | 📋 设计中 |
| P1 | K 池多重比较无校正 | 🔴 严重 | **P28** | 📋 设计中 |
| P2 | Simpson 悖论风险 | 🔴 严重 | **P28** | 📋 设计中 |
| P3 | Laplace 过度收缩 | 🟡 中等 | **P28** | 📋 设计中 |
| P4 | BA 似然比 inf（已知） | 🟡 中等 | **P28** | 📋 设计中 |
| P5 | pseudo_compact 不完整 | 🟡 中等 | **P28** | 📋 设计中 |
| P6 | pool_target_map 回退（已知） | 🟢 低等 | P2 + P28 | ✅/📋 |
| P7 | 资源/兑换池特殊处理 | 🟢 低等 | **P28** | 📋 设计中 |

**全覆盖统计**：两份审查共发现 57 项缺陷/风险 → 已修复 17 项（30%）、已纳入计划 38 项（67%）、已知问题 2 项（3%）。**100% 的缺陷均已在对应计划文件中登记，无遗漏。**

---

## 四、计划间依赖与竞争关系图

```
                    ┌──────────────────────────────────────┐
                    │   图表体系统一：P11 Plotly 迁移 ✅ 完成  │
                    │  （S1-S5 全部完成，S5.3 已提取为 P8）    │
                    │  （P9 matplotlib 重构已放弃）           │
                    └────────────┬─────────────────────────┘
                                 │ S1-S4 已完成 → P8 可执行
                    ┌────────────┼────────────┐
                    ↓            ↓            ↓
              P22 多资源类型   P8 面板合并    （后续所有
               分布（独立计划） 结果展示图表    图表变更条目）
           
独立可执行（无依赖）：
  P2 (过程分析续)    P3 (Bootstrap)      P22 (多资源类型分布)
  P12-Phase1.5 (GUI性能优化)  P13 (调试模式)  P15 (脆弱性理论改进)
  P16 (保底缺陷修复)  P17 (最差影响保底匹配)（均已实施）
  P19 (比较分析面板重构，GDRContext 序列化为其唯一的轻量前置依赖)
  P21 (GDR 分箱与绘图策略实施)
  P25 (EVT改进——离散型与退化分布处理，部分已实施)
  P26 (退路搜索与资源目标搜索理论改进，阶段一独立可执行)
  P27 (转变分析统计严谨性改进)
  P28 (每池GDR分析统计严谨性改进)
  P29 (GDR系统理论文档补充)
  P30 (策略系统与保底理论文档补充)
  P31 (最差影响分析剩余缺陷改进)
  P32 (分布尾部估计改进)

已实施完成（不再出现在执行队列）：
  P7 (project.md 全部 12 条目，含 P0-P2、P4-P7、P9-P13)
  P10 (Worker全局变量消除)
  P11 S1-S5 (Plotly 图表迁移——基础设施 + 8 面板迁移 + 8 项修复 + S5.3 已提取)
  P12-Phase1 (算法优化) + P12-Phase1附加 (脆弱性向量化) + P12-Phase1.5 (GUI性能优化)
  P16 (保底缺陷修复) + P17 (最差影响保底匹配)
  P2 (过程分析续) + P15阶段一 (脆弱性理论改进)
  P3 (Bootstrap引擎，核心完成，UI待做)
  P20 (GUI特有开销优化 P0+P1)
  P23 (统计方法外部库替代实施)
  P24 (EVT 尾部拟合——GPD 集成 + 30 测试 + 零 GUI 改动)
  小型修复 ×6
  GDR扩展（额外）

有依赖链：
  P3 → P4                      (Bootstrap 先于自适应，通用框架→自适应停止)
  P25 → (后续)                 (EVT 退化检测已实现；DGPD 等长期方向待评估)
  P12-Phase2 依赖 P12-Phase1.5 (Numba 内核依赖 Worker 内提取基础设施)
  P12-Phase3 依赖 P12-Phase2   (共享内存依赖数值化数据结构)
  P11 → P22                  (多资源类型图表依赖 Plotly 迁移)
  P11 → P8 面板合并            (面板合并结果展示依赖 Plotly 迁移)
  P17 → P31                  (最差影响保底匹配修复后需重新评估 MAX_POOLS 和逻辑跳跃)
  P26 → P8                   (退路搜索算法修复应先于面板合并——避免固化有缺陷的算法)
  P3/P18 → P27 阶段三         (Bootstrap CI 是转变矩阵同时置信域的前置)
  P3/P18 → P28 阶段二         (Bootstrap CI 是 FDR 校正和同时置信域的前置)
  P24/P25 → P32 阶段二        (EVT 尾部拟合是尾部估计改进的前置)
  P3/P18 → P26 阶段二         (Bootstrap CI 是 SPRT 置信区间的前置)
  P24 → P31                  (EVT 尾部拟合改善条件分位数估计精度)
```

### 竞争/冲突关系

| 关系 | 涉及计划 | 说明 |
|------|---------|------|
| **已解决** | ~~P9 ↔ P11~~ | 2026-05-25 已选定 P11（Plotly 迁移），P9 已放弃 |
| **先后顺序** | P3 → P4 | P3（Bootstrap）先实现通用重抽样框架，P4（自适应+EVT）在此基础上扩展 |
| **先后顺序** | P11 → P22 | S5.3 已提取为 P22 独立计划，P11 S1-S4 为 P22 的基础设施前置（已完成） |
| **先后顺序** | P11 → P8 面板合并 | 面板合并结果展示依赖 Plotly 迁移（已完成） |
| **先后顺序** | P12 P1→P2→P3 | 性能优化三阶段严格串行，每阶段独立交付 |
| **先后顺序** | P26 → P8 | 退路搜索算法修复（P26 阶段一）应先于面板合并（P8）——避免在合并代码中固化有缺陷的算法 |
| **先后顺序** | P3/P18 → P27/P28 | Bootstrap CI 基础设施是转变分析和每池GDR分析阶段二/三的前置 |
| **先后顺序** | P24/P25 → P32 | EVT 尾部拟合基础设施是分布尾部估计改进阶段二的前置 |
| **先后顺序** | P17 → P31 | 最差影响保底匹配修复后需重新评估 MAX_POOLS 截断和逻辑跳跃 |

---

## 五、建议执行顺序

> **已做出的决策**（2026-05-25）：图表体系统一采用 **P11（Plotly 迁移）**，P9（matplotlib 重构）已放弃。
> **理论审查驱动**（2026-05-26）：P16、P17 为审查发现的严重缺陷修复，建议最优先执行。

```
第1轮 🔴 紧急修复（可并行，互不依赖）：
  P16（保底缺陷修复）| P17（最差影响保底匹配）
         ↓
第2轮 🟡 高优先级（可并行）：
  P15 阶段一（脆弱性 r_grid+LLR+逻辑回归）| P2（过程分析续）|
  P3（Bootstrap）| 小型修复（resource_per_card inf→nan 等 6 项）|
  P19 第一阶段（ResultStore + 数据管理 Tab + MainWindow 集成）
         ↓
第3轮 🟠 中优先级——理论文档补充（全部可并行，代码改动量小）：
  P29（GDR 理论文档补充 1-2 天）| P30（策略与保底文档补充 1 天）|
  P32 阶段一（分布尾部 UI 标注 0.5 天）
         ↓
第4轮 🟠 中优先级——统计严谨性改进（部分有依赖）：
  P3 → P4（自适应+EVT）| P13（调试模式）|
  P19 第二阶段（比较分析面板 + ComparisonAnalyzer 引擎）|
  P26 阶段一（退路搜索 UI 降级 + 上界统一 2-3 天）|
  P27 阶段一（转变分析 CI + 连续度量 2-3 天）|
  P28 阶段一（Simpson 诊断 + inf 修复 1-2 天）|
  P31 阶段一（最差影响 UI 标注 + 截断检测 1 天）
         ↓
第5轮 🔵 依赖链下游：
  P11 S5 → P22（多资源类型分布）+ P8（面板合并，依赖 P26 阶段一）|
  P3/P18 → P26 阶段二（SPRT 二分搜索）|
  P3/P18 → P27 阶段二（马尔可夫诊断 + FDR）|
  P3/P18 → P28 阶段二（FDR + 经验贝叶斯 + pseudo_compact）|
  P24/P25 → P32 阶段二（Bootstrap SE + EVT 尾部委托）
         ↓
第6轮 🟣 长期深化：
  P12-Phase2 → P12-Phase3（性能优化深化）
  P15 阶段二、三（带宽选择、Wild Bootstrap、自适应带宽）
  P26 阶段三（ε-Pareto 前沿）| P31 阶段二/三（敏感度分析 + 可配置池序列）
```

**并行度最大化**：第 1-2 轮已完成；第 3 轮 3 项文档补充计划全部可并行（<3 天合计）；第 4 轮 7 项阶段一改进互不依赖；第 5 轮有依赖链但各分支独立推进。

**关键路径**：P3/P18（Bootstrap CI）→ P26/P27/P28 阶段二（统计严谨性改进）。Bootstrap CI 基础设施对所有「不确定性量化」改进至关重要。

**快赢项**：P29（GDR 文档 <2 天）、P30（策略文档 1 天）、P32 阶段一（尾部 UI <0.5 天）、P31 阶段一（最差影响 UI 1 天）——这些计划几乎不需要算法改动，仅通过文档化和 UI 标注即可显著提升理论透明度。建议在新一批实施中优先执行，在最短时间内消除审查报告中大部分「设计中」状态。

**已实施完成（移出执行队列）**：P10（Worker 全局变量消除）、P14（最差影响分析修复）、P12-Phase1 + Phase1附加 + Phase1.5（算法优化 + 脆弱性向量化 + GUI 性能优化）、P16（保底缺陷修复）、P17（最差影响保底匹配）、P2（过程分析续）、P15 阶段一（脆弱性理论改进）、P3（Bootstrap 引擎，核心完成 UI 待做）、小型修复 ×6、GDR 扩展（额外）、P7（project.md 全部 12 条目，P8 已提取为独立计划）、P11（Plotly 图表迁移 S1-S5，S5.3 已提取为独立计划）。

---

## 六、已知问题（与计划无关的独立 bug/限制）

- **专武角色比始终为 0**：`GDRContext.weapon_character_map` 无配置入口，需在配置系统新增角色-武器对应表
- **per_pool_analysis.py 的 success_func**：仍使用内联逻辑（InfoVector 路径），未改用 SuccessChecker
- **转变分析不显示结果图片**（2026-05-24 发现 → 2026-05-25 修复）：
  - 根因：① 分析工人在转变分析中从未使用 streaming 提取器预计算的 `transition_flags`，而是从 `cumulative_snapshots` 重新计算，守卫条件可能静默跳过 ② `_update_transition()` 计数逻辑有 bug——使用 `sum(target_specs.values())` 做总数比对，忽略了逐卡独立需求量（如需要 2A+1B，抽到 3A 也会被标记为成功）
  - 修复：
    - `streaming.py:301-321`：`_update_transition` 改为逐卡检查 `cumulative[card_id] >= quantity_needed`
    - `analysis_panel.py:1337-1374`：优先使用预计算的 `transition_flags`，回退到 `cumulative_snapshots` 重新计算，两数据源均为空时给出明确警告「缺少数据——transition_flags 和 cumulative_snapshots 均为空」
  - 测试：新增 2 项逐卡需求验证测试 + 2 项完整管线测试（`tests/core/test_transition.py`）

---

## 七、已完成计划的归档

以下计划已完成或仅剩可忽略的低优先级条目，设计文档和计划文件仅作参考，可归档：

- 2026-05-07-gacha-simulator-implementation-plan.md
- 2026-05-13-code-refactor.md
- 2026-05-13-retreat-search.md
- 2026-05-13-retreat-vulnerability-analysis.md
- 2026-05-13-strategy-comparison-infra.md
- 2026-05-14-worst-impact.md
- 2026-05-14-retreat-search-phase2.md（部分完成）
- 2026-05-16-gdr-unification.md
- 2026-05-16-streaming-refactor.md
- `docs/策略代码调查与重构方案.md`（v9，2项遗留归入 P6，其余已确认完成；2026-05-25 已移入 `docs/archive/`，步骤 4.4 策略参数动态控件搁置条目已提取至 `docs/搁置计划记录.md`）
- `docs/modification_plan.md`（全部条目已完成）
- `docs/streaming_memory_optimization_plan.md`（方案A已实施，B已放弃，C已搁置）
- `docs/Worker全局变量消除方案.md`（P10，全部完成；2026-05-26 归档）
- `docs/最差影响分析修复方案.md`（P14，修复1/2/4已完成，修复3已搁置；2026-05-26 归档）
- `docs/图表绘制代码重构方案.md`（P9，已放弃，被P11替代；2026-05-26 归档）
- `docs/项目条目实施计划与调查报告.md`（P7，全部 12 条目已完成，P8 已提取为独立计划；2026-05-27 归档）
- `docs/Plotly图表迁移实施方案.md`（P11，S1-S5 全部完成，S5.3 已提取为独立计划，第八章已搁置；2026-05-27 归档）
- docs/archive/EVT尾部拟合独立计划.md（P24，已完成并归档；2026-05-28）
- `docs/archive/自适应模拟与方差缩减计划-v2.md`（P4 v2 实施计划，被 v3 替代；2026-05-28 归档）
- `docs/archive/自适应模拟与方差缩减-设计文档.md`（P4 设计文档，已合并入 v3 实施计划；2026-05-28 归档）

---

## 八、调查报告目录

以下为纯研究/调查报告（非实施计划），已移入 `docs/reports/`：

| 文件 | 说明 |
|------|------|
| `docs/reports/GDR分箱与绘图策略报告.md` | 17种GDR的离散性分类、分箱与绘图策略研究 |
| `docs/reports/最差后期影响调查报告.md` | 最差后期影响完整流程调查（仅报告，不涉及编码） |
| `docs/reports/全模块理论严谨性系统性审查.md` | 全部已实现模块+未实施计划的理论严谨性审查（28 缺陷 + 5 计划风险） |
| `docs/reports/补充模块理论严谨性审查.md` | 4 个补充模块的理论严谨性审查（24 缺陷）——最好/最差情形、转变分析、资源/目标搜索、每池GDR |
