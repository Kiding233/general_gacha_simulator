# 图表绘制代码重构方案

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 统一 ~1110 行散落在 10 个文件中的 matplotlib 绘图代码，新建 `visualization/chart_utils.py`，消除 3 种重复模式，修复 3 处临时文件泄漏。

**Architecture:** 提取公共绘图基础设施为独立模块（`chart_utils.py`），提供初始化辅助、PNG→QPixmap 显示辅助、统一图表函数三层 API。逐面板迁移，每个面板迁移后目视验证图表输出不变。

**Tech Stack:** Python 3.10+, matplotlib, PyQt6, pytest

> 日期：2026-05-23 | 状态：计划完成，未实施
> 来源：project.md 条目5e 图表统一工具部分
> 被依赖：`docs/项目条目实施计划与调查报告.md` 的 P6-P8（抽数换算/不抽卡参考线/多资源类型）、`docs/面板合并可行性计划.md` 的结果展示图表

---

## 一、现状调查

### 1.1 总览

项目共有 **10 个文件** 包含 matplotlib 绘图代码，约 **1110 行**。所有图表均采用「**matplotlib → 临时 PNG → QPixmap → QLabel**」模式，没有使用 `FigureCanvas` 或 Qt 内联后端。

### 1.2 按文件统计

| 文件 | 绘图函数 | 行数 | 图表类型 | 临时文件 |
|------|---------|------|---------|---------|
| `gui/analysis_panel.py` | `ChartAnalysisWorker.run()` | ~600 | 直方图/CDF/Ridge/热力图/3D瀑布/2D瀑布/条形图/散点图/相关性矩阵/转变分析 | 持久化到 `output_dir/` |
| `core/vulnerability.py` | `plot_vulnerability_ridge()` | 136 | Ridge 山脊图 (多池阶梯填充) | `NamedTemporaryFile(delete=False)` |
| `core/vulnerability.py` | `plot_vulnerability()` | 80 | 双面板：堆叠直方图+条件概率曲线 | `NamedTemporaryFile(delete=False)` |
| `gui/resource_search_panel.py` | `_draw_resource_chart()` | 73 | 资源搜索过程散点/折线混合图 | `mktemp` (已清理) |
| `gui/strategy_panel.py` | `_draw_strategy_chart()` | 60 | 策略成功率折线图 | `mktemp` (已清理) |
| `gui/retreat_search_panel.py` | `_plot_pareto_chart()` | 50 | Pareto 前沿散点图 | `NamedTemporaryFile(delete=False)` |
| `gui/process_analysis_panel.py` | `_plot_cond_dist()` | 53 | 条件GDR分布直方图 | `NamedTemporaryFile(delete=False)` (已清理) |
| `gui/worst_impact_panel.py` | `_plot_distribution()` | 41 | 新池子数概率分布条形图 | `NamedTemporaryFile(delete=False)` |
| `gui/retreat_panel.py` | `display_results()` | ~30 | 显示 vulnerability.py 生成的 PNG | 无 (接收路径) |
| `visualization/font_config.py` | `configure_chinese_font()` | 26 | 全局字体配置 (被所有绘图调用) | 无 |

### 1.3 重复模式

以下模式在多个文件中逐字重复：

#### 模式1：导入 + 后端设置 + 字体配置（8处）

```python
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
from gacha_simulator.visualization.font_config import configure_chinese_font
configure_chinese_font()
```

出现在：`analysis_panel.py:346-348`、`vulnerability.py:383/521`、`resource_search_panel.py:598`、`strategy_panel.py:666`、`retreat_search_panel.py:233`、`process_analysis_panel.py:709`、`worst_impact_panel.py:457`

#### 模式2：savefig + QPixmap 加载 + 缩放 + 显示（6处）

```python
fig.savefig(tmp_path, dpi=200, bbox_inches='tight', pad_inches=0.15)
plt.close(fig)
pixmap = QPixmap(tmp_path)
scaled = pixmap.scaled(target_size, Qt.AspectRatioMode.KeepAspectRatio,
                       Qt.TransformationMode.SmoothTransformation)
label.setPixmap(scaled)
```

出现在：`analysis_panel.py:224-227`、`process_analysis_panel.py:742-746`、`resource_search_panel.py:660-662`、`retreat_search_panel.py:588-593`、`strategy_panel.py:715-717`、`worst_impact_panel.py:485-490`

#### 模式3：Ridge 图实现（2处，~100行/处）

`vulnerability.py:373-508` 和 `analysis_panel.py:1085-1220` 各自实现了几乎完全相同的 ridge 图逻辑：
- 全局统一 bin 边界
- `ax.step` + `ax.fill_between` 绘制阶梯填充
- `ax.axvline` 标记均值
- 垂直堆叠子图

### 1.4 临时文件泄漏

| 文件 | 泄漏？ | 原因 |
|------|--------|------|
| `vulnerability.py` | **是** | 返回路径，调用方 `retreat_panel.py` 未清理 |
| `retreat_search_panel.py` | **是** | `delete=False` 且无清理代码 |
| `worst_impact_panel.py` | **是** | `delete=False` 且无清理代码 |
| `analysis_panel.py` | 否 | 持久化到 `output_dir/`，有意为之 |
| `strategy_panel.py` | 否 | `os.unlink(tmp)` 清理 |
| `resource_search_panel.py` | 否 | `os.unlink(tmp)` 清理 |
| `process_analysis_panel.py` | 否 | `try/finally os.unlink` 清理 |

### 1.5 缩放变体

| 变体 | 使用位置 |
|------|---------|
| `scaledToWidth(900, SmoothTransformation)` | `analysis_panel.py`、`retreat_panel.py` |
| `scaled(label.size(), KeepAspectRatio, SmoothTransformation)` | `resource_search_panel.py`、`strategy_panel.py`、`worst_impact_panel.py` |
| `scaled(max_w, max_h, KeepAspectRatio, SmoothTransformation)` | `retreat_search_panel.py`、`process_analysis_panel.py` |

### 1.6 图表类型清单

| 图表类型 | 出现次数 | 文件 |
|---------|---------|------|
| 直方图 (`ax.hist`) | 8 | analysis_panel (5), vulnerability (2), process_analysis_panel (1) |
| 折线图 (`ax.plot`) | 6 | analysis_panel (3), strategy_panel, resource_search_panel, retreat_panel |
| Ridge 图 (`ax.step`+`ax.fill_between`) | 2 | vulnerability, analysis_panel |
| 条形图 (`ax.bar`/`ax.barh`) | 4 | analysis_panel (3), worst_impact_panel (1) |
| 散点图 (`ax.scatter`) | 2 | analysis_panel, retreat_search_panel |
| 热力图 (`ax.imshow`) | 3 | analysis_panel (3) |
| 3D 图 (`mplot3d`) | 1 | analysis_panel |
| 参考线 (`axvline`/`axhline`/`axvspan`) | ~25 | 全部文件 |

---

## 二、测试基线

重构前应先建立绿色基线——确保现有测试全部通过，重构中每步完成后重新跑测试以尽早发现回归。

### 基线确认

```bash
# 重构前：全部测试必须通过
pytest -v
# 记录输出，确认无 FAIL
```

### 需要关注的现有测试

- `tests/visualization/test_plots.py` — 当前被 `pytest.skip` 跳过（引用的函数尚未实现），S6 将激活它
- `tests/core/test_vulnerability.py` — 间接触及 `plot_vulnerability_ridge()` 和 `plot_vulnerability()` 的调用路径

### 重构中的测试策略

| 阶段 | 测试方式 | 验证内容 |
|------|---------|---------|
| S1 | TDD（写测试→实现→通过） | `chart_utils.py` 每个函数的单元测试 |
| S2 | TDD | `plot_ridge()`、`plot_histogram()` 等统一图表函数 |
| S3-S5 | 目视 + pytest | 迁移后面板图表输出与重构前一致；pytest 保持全绿 |
| S6 | pytest | `test_plots.py` 不再 skip，全部通过 |

---

## 三、回滚策略

每个面板迁移独立提交。如果某面板迁移后发现图表异常：

1. **回滚该面板的提交**（`git revert`），不影响已迁移的其他面板
2. **定位问题**：对比迁移前后的 PNG 输出（如有差异，存为文件比较）
3. **修复后重新迁移该面板**

关键原则：**一个面板一个提交**，不合并多个面板的迁移。

---

## 四、重构方案

### 总体迁移顺序

```
S1: 新建 chart_utils.py（TDD）→  S2: 统一图表函数测试（TDD）
                                        ↓
S3: 迁移简单面板（3个，每个~10行变更）→ 目视验证 → 提交
                                        ↓
S4: 迁移 vulnerability.py（~100行删除）→ 目视验证 → 提交
                                        ↓
S5: 迁移 analysis_panel.py（13种图表，逐个迁移）→ 逐图表目视验证 → 提交
                                        ↓
S6: 修复 test_plots.py → 全量测试 → 完成
```

### 新建模块 API 设计

`gacha_simulator/visualization/chart_utils.py` 提供三层 API：

**A. 初始化辅助**

```python
def setup_mpl() -> None:
    """聚合 matplotlib.use('Agg') + configure_chinese_font()，幂等调用。"""
```

**B. PNG → QPixmap 显示辅助**

```python
def figure_to_pixmap(fig: Figure, target_widget: QWidget, dpi: int = 200,
                     cleanup: bool = True) -> QPixmap:
    """fig.savefig 到临时 PNG → QPixmap 加载 → 缩放适配 widget → 可选清理。
    缩放策略：优先适配 widget 宽度（scaledToWidth），KeepAspectRatio + SmoothTransformation。"""

def figure_to_file(fig: Figure, path: str | Path, dpi: int = 200) -> None:
    """保存到指定路径（用于 analysis_panel 持久化图表）。"""
```

**C. 统一图表函数**

```python
def smart_hist_params(samples) -> dict:
    """（提取自 AnalysisPanel._hist_params()）
    - 单值 → bins=[v-0.5, v+0.5], density=False
    - 离散整数 → bins=range(lo, hi+1), density=False
    - 连续 → freedman_diaconis_bins, density=True
    - 尖峰(IQR/span < 0.05) → 强制最多 200 bins"""

def plot_histogram(ax, samples, label=None, color='steelblue', alpha=0.7,
                   mean_line=True, quantile_lines=None, **kwargs) -> None:
    """统一单变量直方图。自动选择 bins（复用 smart_hist_params）。"""

def plot_cdf(ax, samples, label=None, color='steelblue', **kwargs) -> None:
    """统一经验 CDF 曲线。"""

def plot_ridge(data_dict: Dict[str, np.ndarray], figsize=None,
               global_bins=True, mean_lines=True, **kwargs) -> Figure:
    """统一 Ridge 图。data_dict = {label: samples}。
    兼容两种输入格式：vulnerability.py 的 resource_values_all/failed 和
    analysis_panel.py 的 cumulative_snapshots。"""

def add_reference_line(ax, value, orientation='v', label=None,
                       color='red', linestyle='--', **kwargs) -> None:
    """统一添加参考线。"""
```

### S1 — 新建 chart_utils.py（TDD）

> **S1 分为 5 个子任务，每个遵循 TDD：写失败测试 → 最小实现 → 通过。**

- [ ] **S1.1: setup_mpl()**

**新建：** `gacha_simulator/visualization/chart_utils.py`
**新建：** `tests/visualization/test_chart_utils.py`

```python
# 测试
def test_setup_mpl_idempotent():
    import matplotlib
    setup_mpl()
    backend1 = matplotlib.get_backend()
    setup_mpl()  # 第二次调用不报错
    backend2 = matplotlib.get_backend()
    assert backend1 == backend2  # 幂等
```

实现：聚合 `matplotlib.use('Agg')` + `configure_chinese_font()` 调用。

- [ ] **S1.2: smart_hist_params()**

**迁移自：** `gacha_simulator/gui/analysis_panel.py`，`AnalysisPanel._hist_params()` 静态方法

测试用例：单值输入、离散整数、连续数据、尖峰分布（IQR/span < 0.05）。

```python
def test_smart_hist_params_single_value():
    bins, density = smart_hist_params([5, 5, 5])
    assert bins == [4.5, 5.5]
    assert density is False

def test_smart_hist_params_discrete():
    bins, density = smart_hist_params([1, 2, 2, 3, 3, 3])
    assert density is False
    # bins 应为 range(1, 4) 即 [1, 2, 3]

def test_smart_hist_params_continuous():
    bins, density = smart_hist_params(np.random.normal(0, 1, 1000))
    assert density is True
```

- [ ] **S1.3: figure_to_pixmap()**

测试：创建简单 Figure → 调用 `figure_to_pixmap()` → 返回有效 QPixmap → 临时文件已清理。

```python
def test_figure_to_pixmap_returns_valid_pixmap(qtbot):
    fig, ax = plt.subplots()
    ax.plot([1, 2, 3])
    widget = QLabel()
    pixmap = figure_to_pixmap(fig, widget, dpi=100, cleanup=True)
    assert isinstance(pixmap, QPixmap)
    assert not pixmap.isNull()
    plt.close('all')

def test_figure_to_pixmap_cleanup(qtbot):
    import tempfile, os
    fig, ax = plt.subplots()
    widget = QLabel()
    # 验证 cleanup=True 时临时文件被删除
    figure_to_pixmap(fig, widget, cleanup=True)
    # 临时目录应为空
```

- [ ] **S1.4: figure_to_file()**

```python
def test_figure_to_file(tmp_path):
    fig, ax = plt.subplots()
    ax.plot([1, 2, 3])
    out = tmp_path / "test.png"
    figure_to_file(fig, out)
    assert out.exists()
    plt.close('all')
```

- [ ] **S1.5: 运行 S1 全部测试并提交**

```bash
pytest tests/visualization/test_chart_utils.py -v
# 预期: 全部 PASS
```

```bash
git add gacha_simulator/visualization/chart_utils.py tests/visualization/test_chart_utils.py
git commit -m "feat: 新建 chart_utils.py——setup_mpl/smart_hist_params/figure_to_pixmap/figure_to_file"
```

### S2 — TDD 统一图表函数

- [ ] **S2.1: plot_histogram()**

测试：验证 bins 选择、mean_line 位置、quantile_lines 正确绘制。

- [ ] **S2.2: plot_cdf()**

测试：验证 CDF 曲线从 0 到 1，样本点映射正确。

- [ ] **S2.3: plot_ridge()**

测试：验证全局 bin 一致性、子图数量、均值线位置。需覆盖两种输入格式（`resource_values_all/failed` 和 `cumulative_snapshots`）。

- [ ] **S2.4: add_reference_line()**

测试：验证竖线/横线位置、标签、颜色。

- [ ] **S2.5: 提交**

```bash
git add gacha_simulator/visualization/chart_utils.py tests/visualization/test_chart_utils.py
git commit -m "feat: chart_utils 添加 plot_histogram/plot_cdf/plot_ridge/add_reference_line"
```

### S3 — 迁移简单面板

> 每个面板独立提交。迁移内容：替换模式1（导入+字体）+ 模式2（savefig→QPixmap）。

- [ ] **S3.1: 迁移 worst_impact_panel.py**

**文件：** `gacha_simulator/gui/worst_impact_panel.py`

替换 `_plot_distribution()` 中的导入/后端/字体模式 + savefig→QPixmap 模式。修复临时文件泄漏。

验证：启动 GUI → 最差影响 Tab → 执行分析 → 图表显示正常。

```bash
git add gacha_simulator/gui/worst_impact_panel.py
git commit -m "refactor: worst_impact_panel 使用 chart_utils 统一绘图"
```

- [ ] **S3.2: 迁移 process_analysis_panel.py**

**文件：** `gacha_simulator/gui/process_analysis_panel.py`

替换 `_plot_cond_dist()` 中的导入/字体模式 + savefig→QPixmap 模式。

验证：启动 GUI → 过程分析 Tab → 执行分析 → 条件分布图显示正常。

```bash
git add gacha_simulator/gui/process_analysis_panel.py
git commit -m "refactor: process_analysis_panel 使用 chart_utils 统一绘图"
```

- [ ] **S3.3: 迁移 resource_search_panel.py**

**文件：** `gacha_simulator/gui/resource_search_panel.py`

替换 `_draw_resource_chart()` 中的导入/字体模式 + savefig→QPixmap 模式。

验证：启动 GUI → 最少资源 Tab → 执行搜索 → 散点/折线图显示正常。

```bash
git add gacha_simulator/gui/resource_search_panel.py
git commit -m "refactor: resource_search_panel 使用 chart_utils 统一绘图"
```

- [ ] **S3.4: 迁移 strategy_panel.py**

**文件：** `gacha_simulator/gui/strategy_panel.py`

替换 `_draw_strategy_chart()` 中的导入/字体模式 + savefig→QPixmap 模式。

验证：启动 GUI → 策略比较 Tab → 执行比较 → 折线图显示正常。

```bash
git add gacha_simulator/gui/strategy_panel.py
git commit -m "refactor: strategy_panel 使用 chart_utils 统一绘图"
```

- [ ] **S3.5: 迁移 retreat_search_panel.py**

**文件：** `gacha_simulator/gui/retreat_search_panel.py`

替换 `_plot_pareto_chart()` 中的导入/字体模式 + savefig→QPixmap 模式。修复临时文件泄漏。

验证：启动 GUI → 退路分析 Tab → 执行分析 → Pareto 散点图显示正常。

```bash
git add gacha_simulator/gui/retreat_search_panel.py
git commit -m "refactor: retreat_search_panel 使用 chart_utils，修复临时文件泄漏"
```

### S4 — 迁移 vulnerability.py

- [ ] **S4.1: 替换 plot_vulnerability_ridge() 为 plot_ridge()**

**文件：** `gacha_simulator/core/vulnerability.py`

`plot_vulnerability_ridge()` (line 373-508, ~136行) 替换为调用 `plot_ridge()`。适配 `resource_values_all/failed` 数据结构。

- [ ] **S4.2: 拆分 plot_vulnerability() 双面板**

**文件：** `gacha_simulator/core/vulnerability.py`

`plot_vulnerability()` (line 521-601, ~80行) 的堆叠直方图 + 条件概率曲线拆分为两个统一函数调用（`plot_histogram(stacked=True)` + `plot_cdf`）。

- [ ] **S4.3: 替换导入/字体模式 + savefig→QPixmap 模式**

两个函数中的模式1和模式2替换为 `setup_mpl()` + `figure_to_pixmap()`。修复临时文件泄漏（两处 `delete=False` 改为 `cleanup=True`）。

- [ ] **S4.4: 目视验证 + 提交**

验证：启动 GUI → 退路分析 Tab → 执行分析 → Ridge 图和双面板图显示正常。

```bash
git add gacha_simulator/core/vulnerability.py
git commit -m "refactor: vulnerability.py 使用 chart_utils，删除 ~100行重复 ridge 代码，修复临时文件泄漏"
```

### S5 — 迁移 analysis_panel.py（最复杂）

> **analysis_panel.py 的 `ChartAnalysisWorker.run()` 包含 13 种图表。按图表类型逐个迁移，每个图表迁移后目视验证，独立提交。**

**关键约束：** `ChartAnalysisWorker.run()` 中图表输出到 `output_dir/` 供"导出"功能使用，重构后仍需保持此行为——使用 `figure_to_file()` 而非 `figure_to_pixmap()`。

- [ ] **S5.1: 替换导入/后端/字体模式**

将 `analysis_panel.py` 中的模式1（约 line 346-348）替换为 `from gacha_simulator.visualization.chart_utils import setup_mpl; setup_mpl()`。

- [ ] **S5.2: 迁移直方图（5处）→ plot_histogram()**

逐一替换 5 处 `ax.hist()` 调用为 `plot_histogram()`。每替换一处即运行分析面板确认直方图显示不变。

- [ ] **S5.3: 迁移 CDF（3处）→ plot_cdf()**

- [ ] **S5.4: 迁移 Ridge 图（1处）→ plot_ridge()**

替换 line 1085-1220 的 ridge 实现。适配 `cumulative_snapshots` 数据结构。

- [ ] **S5.5: 迁移条形图（3处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.6: 迁移散点图（1处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.7: 迁移热力图（3处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.8: 迁移 3D 图（1处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.9: 迁移折线图（3处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.10: 迁移相关性矩阵（1处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.11: 迁移转变分析（1处）→ 使用统一的 rcParams + figure_to_file()**

- [ ] **S5.12: 提取 _hist_params 静态方法**

`AnalysisPanel._hist_params()` 已在 S1.2 提取为 `smart_hist_params()`。此步将 `analysis_panel.py` 中的调用替换为 `smart_hist_params()`，删除原有静态方法。

- [ ] **S5.13: 运行全量测试 + 目视验证全部图表**

启动 GUI → 统计分析 Tab → 执行分析 → 逐一确认 13 种图表显示正常；确认导出功能正常（`output_dir/` 中 PNG 文件可打开且内容正确）。

```bash
pytest -v
# 预期: 全部 PASS
```

```bash
git add gacha_simulator/gui/analysis_panel.py
git commit -m "refactor: analysis_panel 13种图表全部迁移至 chart_utils，删除 ~200行重复代码"
```

### S6 — 修复 test_plots.py

- [ ] **S6.1: 激活 test_plots.py**

**文件：** `tests/visualization/test_plots.py`

删除 `pytest.skip()` 行。将测试中的函数调用（`plot_pmf`/`plot_cdf`/`plot_time_series`）更新为 `chart_utils.py` 中实际存在的函数名：

```python
from gacha_simulator.visualization.chart_utils import plot_histogram, plot_cdf

def test_plot_histogram():
    fig, ax = plt.subplots()
    plot_histogram(ax, [1, 1, 2, 2, 2, 3])
    assert fig is not None

def test_plot_cdf():
    fig, ax = plt.subplots()
    plot_cdf(ax, [1, 2, 3, 4, 5])
    assert fig is not None
```

- [ ] **S6.2: 运行全量测试**

```bash
pytest tests/visualization/ -v
# 预期: 全部 PASS，无 skip
```

- [ ] **S6.3: 提交**

```bash
git add tests/visualization/test_plots.py
git commit -m "test: 激活 test_plots.py，测试 chart_utils 统一图表函数"
```

---

## 五、实施后效果

| 指标 | 重构前 | 重构后 |
|------|--------|--------|
| 绘图代码总行数 | ~1110 | ~700 (-37%) |
| 导入/字体/后端重复 | 8处 各4行 | 1处 1行 |
| PNG→QPixmap 重复 | 6处 各5行 | 1处 1行 |
| Ridge 图重复 | 2处 各~100行 | 1处 |
| 临时文件泄漏 | 3处 | 0 |
| 新增图表类型 | 需修改多处 | 仅修改 chart_utils.py |
| 测试覆盖 | 0（test_plots.py 被 skip） | chart_utils.py 有完整单元测试 |

### 对 project.md 其他条目的支撑

图表重构完成后，P6（抽数换算）、P7（不抽卡参考线）、P8（多资源类型分布）的图表变更将简化为：
- P6：调用 `plot_resource_distribution(cost_per_draw=N)`
- P7：调用 `add_reference_line(value)` / 叠加不抽卡分布
- P8：调用 `plot_resource_distribution(resource_label='starlight')` 等
