# 应用打包首版实施计划

> **目标:** PyInstaller 文件夹分发（onedir），用户双击 `.exe` 即启动 GUI，无需安装 Python。
> **设计文档:** [00-档案](./00-档案.md) · [01-理论](./01-理论.md) · [02-实施](./02-实施.md)
> **状态:** ✅ 已完成（2026-06-04）—— 8 个任务全部完成，产物 `dist/GachaStat/` (~714MB)

**目标:** 生成自包含的 `dist/GachaStat/` 文件夹，含 `GachaStat.exe` + 所有依赖。

**技术栈:** Python 3.10+ / PyInstaller 6.x / 路径适配 `sys._MEIPASS` / multiprocessing freeze_support

**方案选择:** PyInstaller（非 Nuitka/Briefcase）——构建快 10-20 倍，运行时性能差距 <5%，QtWebEngine 成熟。

**预计产物体积:** 180-250MB（QtWebEngine Chromium 占 ~120MB）

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `gacha_simulator/paths.py` | **新建** | 路径解析工具——只读路径（config/resources）+ 可写路径（用户数据目录） |
| `gacha_simulator/__init__.py` | **新建** | 空文件——将隐式命名空间包转为显式包，消除 PyInstaller 风险 |
| `gacha_simulator/main.py` | **修改** | +3行：`freeze_support()` + `_ICON_PATH` 改用 `paths` |
| `gacha_simulator/gui/main_window.py` | **修改** | ~4行：`_DEFAULT_CONFIG_DIR` + `_ICON_PATH` 改用 `paths` |
| `gacha_simulator/gui/analysis_panel.py` | **修改** | ~2行：`output_dir` 改用 `paths.get_user_data_dir()` |
| `gacha_simulator/gui/templates/chart_container.html` | **删除** | 死代码——实际使用内联 `_SKELETON_HTML` |
| `GachaStat.spec` | **新建** | PyInstaller 构建配置——入口点、数据文件、隐藏导入、排除项 |

---

## 任务 1: 创建 `gacha_simulator/__init__.py`——消除命名空间包风险

**文件:** 创建 `gacha_simulator/__init__.py`

- [ ] **步骤 1: 写入空文件**

`gacha_simulator/` 当前是 PEP 420 隐式命名空间包（无 `__init__.py`），子包（core/gui/service）各有 `__init__.py`。PyInstaller 理论上支持命名空间包，但创建空的 `__init__.py` 可消除不确定性，零风险。

```bash
# 创建空文件
touch gacha_simulator/__init__.py
```

- [ ] **步骤 2: 验证导入不受影响**

```bash
cd d:/Users/DELL/Desktop/gacha_simulator/抽卡模拟与分析程序 && python -c "
import gacha_simulator
print('__init__.py present:', hasattr(gacha_simulator, '__file__') and gacha_simulator.__file__ is not None)
from gacha_simulator.core import Pool, GachaState
print('Subpackage import OK')
"
```

- [ ] **步骤 3: 提交**

```bash
git add gacha_simulator/__init__.py
git commit -m "feat: 添加 __init__.py——显式包声明，消除 PyInstaller 命名空间包风险"
```

---

## 任务 2: 创建 `gacha_simulator/paths.py`——路径解析工具

**文件:** 创建 `gacha_simulator/paths.py`

- [ ] **步骤 1: 写入路径工具模块**

```python
# gacha_simulator/paths.py
"""路径解析工具——同时支持开发环境和 PyInstaller 打包环境。

只读路径（捆绑资源）：
    get_config_dir()           → config/
    get_resource(filename)     → resources/

可写路径（用户数据）：
    get_user_data_dir(*subdirs) → 开发: 包内子目录 / 打包: %APPDATA%/GachaStat/
"""

import os
import sys


def get_base_dir() -> str:
    """返回应用根目录（只读——捆绑的资源文件）。

    开发环境：paths.py 所在目录（= gacha_simulator/ 包目录）
    打包环境：sys._MEIPASS（PyInstaller 的 _internal/ 目录）
    """
    if getattr(sys, 'frozen', False):
        return sys._MEIPASS
    return os.path.dirname(os.path.abspath(__file__))


def get_config_dir() -> str:
    """返回默认配置目录的绝对路径（只读——捆绑的默认配置）。"""
    return os.path.join(get_base_dir(), 'config')


def get_resource(filename: str) -> str:
    """返回 resources/ 目录下指定文件的绝对路径（只读——捆绑的资源）。"""
    return os.path.join(get_base_dir(), 'resources', filename)


def get_user_data_dir(*subdirs: str) -> str:
    """返回用户数据目录的绝对路径（可写——用户生成的数据）。

    开发环境：paths.py 所在目录下的对应子目录
    打包环境：%APPDATA%/GachaStat/ 下的对应子目录
    自动创建目录（exist_ok=True）。

    用法：
        get_user_data_dir()                    → %APPDATA%/GachaStat/
        get_user_data_dir('output')            → %APPDATA%/GachaStat/output/
        get_user_data_dir('output', 'analysis') → %APPDATA%/GachaStat/output/analysis/
    """
    if getattr(sys, 'frozen', False):
        base = os.path.join(os.environ.get('APPDATA', os.path.expanduser('~')), 'GachaStat')
    else:
        base = os.path.dirname(os.path.abspath(__file__))
    target = os.path.join(base, *subdirs)
    os.makedirs(target, exist_ok=True)
    return target
```

- [ ] **步骤 2: 验证开发环境路径**

```bash
cd d:/Users/DELL/Desktop/gacha_simulator/抽卡模拟与分析程序 && python -c "
from gacha_simulator.paths import get_config_dir, get_resource, get_user_data_dir
import os

config = get_config_dir()
print('config_dir:', config)
print('  exists:', os.path.exists(config))

icon = get_resource('app_icon.png')
print('icon_path:', icon)
print('  exists:', os.path.exists(icon))

out = get_user_data_dir('output', 'analysis')
print('user_data:', out)
print('  exists:', os.path.exists(out))
"
```

- [ ] **步骤 3: 提交**

```bash
git add gacha_simulator/paths.py
git commit -m "feat: 新增 paths.py——路径解析工具（只读+可写双路径，开发/打包双环境）"
```

---

## 任务 3: 修改 `gacha_simulator/main.py`——freeze_support + 路径适配

**文件:** 修改 `gacha_simulator/main.py`

- [ ] **步骤 1: 在 `if __name__ == '__main__':` 护卫块首行添加 `freeze_support()`**

```python
if __name__ == '__main__':
    # Windows spawn 模式 + PyInstaller 打包的必要调用
    from multiprocessing import freeze_support
    freeze_support()

    from PyQt6.QtCore import Qt
    # ... 其余不变
```

- [ ] **步骤 2: 替换 `_ICON_PATH` 构造方式**

将：
```python
_ICON_PATH = os.path.join(this_dir, 'gacha_simulator', 'resources', 'app_icon.png')
```

替换为：
```python
from gacha_simulator.paths import get_resource
_ICON_PATH = get_resource('app_icon.png')
```

- [ ] **步骤 3: 提交**

```bash
git add gacha_simulator/main.py
git commit -m "feat: main.py 添加 multiprocessing.freeze_support + 图标路径改用 paths"
```

---

## 任务 4: 修改 `gacha_simulator/gui/main_window.py`——配置路径适配

**文件:** 修改 `gacha_simulator/gui/main_window.py`

- [ ] **步骤 1: 添加导入**

在文件顶部现有导入区域添加：
```python
from gacha_simulator.paths import get_config_dir, get_resource
```

- [ ] **步骤 2: 替换 `_DEFAULT_CONFIG_DIR`**

将（line 34-37）：
```python
_DEFAULT_CONFIG_DIR = os.path.join(
    os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
    'config'
)
```

替换为：
```python
_DEFAULT_CONFIG_DIR = get_config_dir()
```

- [ ] **步骤 3: 替换 `_ICON_PATH`**

将（line 35）：
```python
_ICON_PATH = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), 'resources', 'app_icon.png')
```

替换为：
```python
_ICON_PATH = get_resource('app_icon.png')
```

- [ ] **步骤 4: 提交**

```bash
git add gacha_simulator/gui/main_window.py
git commit -m "feat: main_window 配置/图标路径改用 paths 统一解析"
```

---

## 任务 5: 修改 `gacha_simulator/gui/analysis_panel.py`——输出目录适配

**文件:** 修改 `gacha_simulator/gui/analysis_panel.py`

- [ ] **步骤 1: 添加导入**

```python
from gacha_simulator.paths import get_user_data_dir
```

- [ ] **步骤 2: 替换 output_dir（line 1314）**

将：
```python
self.output_dir = Path(os.getcwd()) / 'output' / 'analysis'
```

替换为：
```python
self.output_dir = Path(get_user_data_dir('output', 'analysis'))
```

- [ ] **步骤 3: 提交**

```bash
git add gacha_simulator/gui/analysis_panel.py
git commit -m "feat: analysis_panel 输出目录改用 paths.get_user_data_dir()"
```

---

## 任务 6: 删除死代码 `gui/templates/chart_container.html`

**文件:** 删除 `gacha_simulator/gui/templates/chart_container.html`

- [ ] **步骤 1: 确认无代码引用**

```bash
cd d:/Users/DELL/Desktop/gacha_simulator/抽卡模拟与分析程序 && grep -r "chart_container" gacha_simulator/ --include="*.py"
# 预期：无输出（仅有 grep 自身可能的匹配）
```

- [ ] **步骤 2: 删除文件**

```bash
git rm gacha_simulator/gui/templates/chart_container.html
```

- [ ] **步骤 3: 提交**

```bash
git commit -m "chore: 删除 chart_container.html 死代码（实际使用内联 _SKELETON_HTML）"
```

---

## 任务 7: 创建 `GachaStat.spec`——PyInstaller 构建配置

**文件:** 创建 `GachaStat.spec`（项目根目录）

- [ ] **步骤 1: 写入 spec 文件**

```python
# -*- mode: python ; coding: utf-8 -*-
"""GachaStat PyInstaller 构建配置 —— 文件夹分发（onedir）模式"""

import sys
from pathlib import Path

_SPEC_DIR = Path(SPECPATH)


a = Analysis(
    ['gacha_simulator/main.py'],

    pathex=[str(_SPEC_DIR)],

    binaries=[],

    datas=[
        ('gacha_simulator/config', 'config'),
        ('gacha_simulator/resources', 'resources'),
    ],

    hiddenimports=[
        # Qt WebEngine
        'PyQt6.QtWebEngineCore',
        'PyQt6.QtWebEngineWidgets',

        # matplotlib 后端
        'matplotlib.backends.backend_qtagg',

        # scipy C 扩展（首版构建后根据 ImportError 补充）
        'scipy.special._ufuncs',
        'scipy.stats._stats',
        'scipy.stats._stats_mstats_common',
        'scipy.optimize._lbfgsb',
        'scipy.optimize._trlib',

        # 项目内部——动态/惰性导入模块
        'gacha_simulator.core.streaming',
        'gacha_simulator.core.comparison_analyzer',
        'gacha_simulator.core.result_store',
        'gacha_simulator.core.process_trace',
        'gacha_simulator.core.process_analysis',
        'gacha_simulator.core.bootstrap',
        'gacha_simulator.core.gdr_analysis',
        'gacha_simulator.core.forward_backward',
        'gacha_simulator.core.vulnerability',
        'gacha_simulator.core.worst_impact',
        'gacha_simulator.core.per_pool_analysis',
        'gacha_simulator.core.risk_analysis',

        # 图表渲染——analysis_panel 函数体内惰性导入
        'gacha_simulator.visualization.chart_spec',
        'gacha_simulator.visualization.plotly_charts',

        # 排期生成器
        'gacha_simulator.generator.schedule_generator',
        'gacha_simulator.generator.target_generator',
    ],

    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],

    excludes=[
        'tkinter',
        'sqlite3',
        'test',
        'unittest',
        'pytest',
        'pdb',
        'distutils',
        'PyQt6.QtBluetooth',
        'PyQt6.QtDBus',
        'PyQt6.QtNfc',
        'PyQt6.QtSensors',
        'PyQt6.QtSerialPort',
        'PyQt6.QtTest',
    ],

    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=None,
    noarchive=False,
)


pyz = PYZ(a.pure, a.zipped_data, cipher=None)


exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='GachaStat',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=str(_SPEC_DIR / 'gacha_simulator' / 'resources' / 'app_icon.png'),
)
```

- [ ] **步骤 2: 安装 PyInstaller**

```bash
pip install pyinstaller
```

- [ ] **步骤 3: 执行首次构建**

```bash
cd d:/Users/DELL/Desktop/gacha_simulator/抽卡模拟与分析程序
pyinstaller GachaStat.spec --noconfirm --clean
```

- [ ] **步骤 4: 检查构建警告**

```bash
# 查看是否有 missing module 警告
cat build/GachaStat/warn-GachaStat.txt | grep -i "missing\|WARNING" | head -40
```

- [ ] **步骤 5: 提交**

```bash
git add GachaStat.spec
git commit -m "feat: 新增 PyInstaller spec——文件夹分发 + QtWebEngine + scipy 隐藏导入"
```

---

## 任务 8: 打包验证

- [ ] **步骤 1: 启动测试**

双击 `dist/GachaStat/GachaStat.exe` → GUI 窗口出现，无控制台黑窗。

- [ ] **步骤 2: 默认配置加载**

检查状态栏显示「已加载默认配置」，所有 Tab 可点击切换。

- [ ] **步骤 3: 批量模拟**

配置 → 批量模拟 → 点击运行 → 观察进度条 + 结果表格正常显示。

- [ ] **步骤 4: 多进程**

在批量模拟面板设置 Workers=4 → 运行 → 模拟成功完成，无报错。

- [ ] **步骤 5: 图表渲染**

切换到统计分析 Tab → 确认 ECDF/直方图等图表在 QWebEngineView 中正常渲染。

- [ ] **步骤 6: 用户数据目录**

运行模拟后检查 `%APPDATA%/GachaStat/output/analysis/` 目录已自动创建。

- [ ] **步骤 7: 配置导入导出**

文件 → 导入配置 → 选择任意配置目录 → 文件 → 另存为 → 确认保存成功。

- [ ] **步骤 8: 图标显示**

窗口图标 + 任务栏图标正常显示。

- [ ] **步骤 9: 关于页面**

帮助 → 关于 → 版本号正确显示。

---

## 验证清单

| # | 测试项 | 预期 | 方法 |
|---|--------|------|------|
| 1 | 启动 | GUI 窗口，无控制台黑窗 | 双击 `GachaStat.exe` |
| 2 | 默认配置 | 自动加载捆绑配置 | 状态栏消息 |
| 3 | 模拟运行 | 批量模拟正常完成 | 进度条 + 结果 |
| 4 | 多进程 | 4 Worker 无报错 | 日志无 freeze_support 错误 |
| 5 | 图表渲染 | Plotly 图表正常 | QWebEngineView 显示 |
| 6 | 用户数据目录 | `%APPDATA%/GachaStat/` 创建 | 资源管理器检查 |
| 7 | 输出文件 | 分析图表可导出 | 统计分析→导出 |
| 8 | 配置导入导出 | QFileDialog 正常 | 文件菜单操作 |
| 9 | 图标 | 窗口+任务栏图标 | 视觉检查 |
| 10 | 版本号 | 正确显示 | 帮助→关于 |
| 11 | 产物体积 | ~700-750MB | `du -sh dist/GachaStat/` |
| 12 | 所有分析可用 | 统计分析/过程分析/策略比较/退路分析/最差影响均正常运行 | 各 Tab 切换并执行分析 |
| 13 | 导入导出功能可用 | 文件→导入配置 + 文件→另存为 + 分析结果导出均正常 | 文件菜单 + 分析面板导出按钮 |

---

## 产物目录结构

```
dist/GachaStat/                       ← 分发给用户的文件夹
├── GachaStat.exe                     ← 双击启动
└── _internal/
    ├── python312.dll
    ├── PyQt6/ Qt6/                   ← Qt 运行时 + WebEngine
    ├── numpy/ scipy/ matplotlib/ plotly/
    ├── gacha_simulator/              ← 应用代码（.pyc）
    ├── config/                       ← 默认配置（只读）
    │   ├── cards.txt, gains.txt, ...
    │   └── pools/
    └── resources/                    ← 图标（只读）
        └── app_icon.png
```

## 运行时目录

```
%APPDATA%/GachaStat/                  ← 首次运行时自动创建
└── output/
    └── analysis/                     ← 分析图表导出
```

---

## 回滚方案

若打包产物运行时出现严重问题：所有改动隔离在新增文件和路径引用处，不影响核心模拟逻辑。可通过以下方式回退：

1. `git revert` 7 个 commit（任务 1-7）
2. 删除 `build/` 和 `dist/` 目录
3. 卸载 `pyinstaller`

核心模拟代码未被修改，不存在数据/算法回归风险。

---

## 后续迭代

| 项目 | 优先级 | 说明 |
|------|--------|------|
| macOS/Linux 打包 | P2 | `paths.py` 接口已就绪，`.spec` 需平台适配 |
| scipy 精确排除 | P2 | 首版构建后根据实际依赖裁剪 |
| CI/CD 自动构建 | P2 | GitHub Actions 自动打包发布 |
| 代码签名 | P2 | EV 证书签名，消除杀软误报 |
| CLI 精简版 | P2 | 无 GUI 版本，体积 ~80MB |
