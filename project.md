阅读并按照以下条目进行搜索、测试、研究、思考，并给出回复或实现计划，最终输出一个总的报告文件。
要求：输出报告文件时将以下条目的回答/计划/方案按依赖关系、实现难度、风险大小排序，仅需报告的放在最前面。

以抽数为单位的资源剩余相关的地方需要标注该抽数由每抽资源比例简单换算，并非实际抽数

整个项目有什么地方是应该使用原始数据但是使用了插值的使用

所有理论上取值不离散的直方图都应该配经验累积分布

为所有配置文件写详细的说明与操作手册，替换“关于”里的相关描述
广泛地为统计数据添加解释文本

理解并系统性重写关于部分的算法说明使在不涉及程序具体实现方法的基础上其尽可能详细
过程分析的条件GDR分布应该叠加在无条件的分布上吗？
条件分布的画图难题：bin宽度和条高度等等不匹配、难读

刻度间隔不够细，但是分箱也不支持？

解释一下现在的备份系统

事件判定和分析相关逻辑应该怎样设计

理论意义/解释：
保底池应该算保底出吗
如何理解兑换池和商店池（资源池）的事件？


基于现有报告，报告最差后期影响的设计思路，重新给出一个设计文件，检查现在的实现是否完全合理

给出一个软件打包方案（性能优先）

热门游戏卡池与保底以及默认配置文件的支持：原神、星铁、绝区零、鸣潮、明日方舟、明日方舟：终末地、异环等。
需要详细调查和对现在的框架下哪些本质上无法实现的理解

bootstrap重抽样次数B如何确定，理论保证？

免费抽和抽卡送免费抽建模与分析问题：
1.一个池子：策略里强制先用免费的、消耗抽数资源（到第多少送抽），和一个限制资源（只送一次）。
2.代码实现抽到次数送免费抽有免费抽的时候必须先用免费抽、必须抽完一个池才能抽另一个，但是每个游戏可能不同所以需要可自由配置，包括免费抽计不计入保底
3.混合方案
4.还有什么方案？
配合策略功能：资源使用优先级？资源使用优先级本身有用吗

策略模块化：基于理论参考.svg
完善具体策略实现，删除不合理的，添加常见的
社区公认的最优策略候选都有哪些？

添加一个无井不下池策略：当当前资源低于能使大保底（必出目标卡的保底）触发所需的抽数（考虑保底计数可能不同）时不抽这个池子，否则就抽。

等待策略等到池子结束前一天以最大化当前资源吗，需要默认这样吗？

置换检验（策略比较的差异检验）

策略比较逻辑重构，应该允许自由比较所有目前可输出的统计量

还需要灵敏度分析吗，应该如何实现？